# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Maintainability] | [src/engine/auditor.py:406] | Lý do: Import 'time' might be unused (AST). AI Note: Trong tệp /tmp/git_audit_qqcb6law/repo/src/engine/auditor.py, dòng 'import time' được khai báo bên trong phương thức 'run' nhưng biến 'time' không được sử dụng ở bất kỳ đâu trong phạm vi phương thức hoặc tệp này. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Performance] | [src/engine/database.py:600] | Lý do: N+1 Query Pattern Detected (DB/API Call inside a loop). Found 'cursor.execute()' inside loop.. AI Note: Trong tệp /tmp/git_audit_qqcb6law/repo/src/engine/database.py, hàm 'save_repositories' thực hiện lặp qua danh sách 'repositories' và gọi 'cursor.execute' cho mỗi phần tử để thực hiện lệnh INSERT. Đây là mô hình N+1 Query điển hình và có thể được tối ưu hóa bằng cách sử dụng 'executemany' để cải thiện hiệu suất. | Trình bày: N_PLUS_ONE | Trọng số: -5.0
- [Reliability] | [src/engine/dependency_checker.py:66] | Lý do: Bắt ngoại lệ quá chung chung và không xử lý gì (Exception hoặc Bare except). AI Note: Trong tệp /tmp/git_audit_qqcb6law/repo/src/engine/dependency_checker.py, khối lệnh bắt ngoại lệ 'Exception' quá chung chung. Mặc dù có ghi chú là bỏ qua lỗi phân tích cú pháp cho các tệp không chuẩn, việc bắt mọi loại lỗi (bao gồm cả lỗi logic hoặc lỗi hệ thống không mong muốn) mà không log lại là một rủi ro về độ tin cậy. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Reliability] | [src/engine/dependency_checker.py:66] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Trong tệp /tmp/git_audit_qqcb6law/repo/src/engine/dependency_checker.py, ngoại lệ được bắt và bỏ qua hoàn toàn bằng lệnh 'pass' mà không có hành động xử lý, ghi log hoặc ném lại lỗi. Ngay cả khi đây là hành động có chủ đích, việc 'nuốt' ngoại lệ trong im lặng mà không ghi lại vết tích để debug sau này vẫn được coi là một lỗi về Reliability. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Reliability] | [src/config.py:85] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Việc bắt mọi Exception và 'pass' khi xử lý cấu hình mà không có log hoặc giá trị mặc định rõ ràng là lỗi Reliability. Nếu 'val' bị None hoặc có kiểu dữ liệu lạ, nó sẽ âm thầm lỗi, gây khó khăn cho việc debug cấu hình. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Maintainability] | [src/engine/auditor.py:387] | Lý do: Function too long (God Object anti-pattern): _step_aggregation (> 150 lines). AI Note: Một hàm dài hơn 150 dòng vi phạm nguyên tắc Single Responsibility và gây khó khăn cho việc bảo trì, kiểm thử. Đây là một True Positive về mặt Maintainability. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Reliability] | [src/engine/auditor.py:371] | Lý do: Bắt ngoại lệ quá chung chung và không xử lý gì (Exception hoặc Bare except). AI Note: Bắt Exception chung chung khi trích xuất snippet code mà không xử lý gì là lỗi. Mặc dù có vẻ là để tránh crash khi hiển thị giao diện, nhưng việc không log lại lỗi (ví dụ: IndexError hoặc FileNotFoundError) khiến lập trình viên không biết tại sao snippet không hiển thị. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Reliability] | [src/engine/auditor.py:371] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Tương tự vi phạm #3, việc sử dụng 'except Exception: pass' là quá rộng và che giấu các vấn đề tiềm ẩn trong logic xử lý chuỗi/mảng. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Reliability] | [src/config.py:59] | Lý do: Bắt ngoại lệ quá chung chung và không xử lý gì (Exception hoặc Bare except). AI Note: Đây là lỗi thật (True Positive). Việc bắt ngoại lệ 'Exception' là quá chung chung (broad exception). Trong đoạn mã này, nếu 'val' không phải là chuỗi (ví dụ: None), nó sẽ gây ra AttributeError. Việc sử dụng Exception có thể vô tình che giấu các lỗi hệ thống hoặc lỗi lập trình nghiêm trọng khác thay vì chỉ xử lý lỗi chuyển đổi dữ liệu cấu hình. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Reliability] | [src/config.py:72] | Lý do: Bắt ngoại lệ quá chung chung và không xử lý gì (Exception hoặc Bare except). AI Note: Đây là lỗi thật (True Positive). Khi thực hiện ép kiểu 'int(val)', mã nguồn chỉ nên bắt các ngoại lệ cụ thể như 'ValueError' hoặc 'TypeError'. Việc bắt toàn bộ 'Exception' vi phạm nguyên tắc Reliability vì nó làm giảm khả năng kiểm soát các lỗi phát sinh không mong muốn. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Reliability] | [src/config.py:85] | Lý do: Bắt ngoại lệ quá chung chung và không xử lý gì (Exception hoặc Bare except). AI Note: Đây là lỗi thật (True Positive). Việc lặp lại lỗi bắt ngoại lệ chung chung cho thấy sự thiếu chặt chẽ trong việc xử lý lỗi. Ngay cả khi hành vi nuốt lỗi là có chủ đích, việc không thu hẹp phạm vi ngoại lệ xuống các lỗi dự kiến (như AttributeError) vẫn là một vi phạm về độ tin cậy của mã nguồn. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Maintainability] | [/src/engine/verification.py:98] | Lý do: Khai báo import thư viện (tempfile, inspect, shutil) nằm bên trong phương thức thay vì ở đầu tệp. Điều này vi phạm PEP8 E402 và có thể gây khó khăn cho việc quản lý dependency. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Reliability] | [/src/engine/verification.py:215] | Lý do: Phương thức run_verification() thu thập kết quả từ subprocess bằng cách lấy dòng cuối cùng của stdout (stdout_lines[-1]). Đây là phương pháp rất mỏng manh vì nếu mã nguồn được quét hoặc các module scanners.py có bất kỳ lệnh 'print' hoặc logger nào ghi ra stdout, dữ liệu JSON sẽ bị đẩy lên trên và việc parse sẽ thất bại hoặc lấy sai dữ liệu. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [/src/engine/verification.py:224] | Lý do: Khả năng xảy ra lỗi 'Resource Leak' nếu subprocess bị treo hoặc bị kill giữa chừng; khối 'finally' cố gắng xóa tệp nhưng không xử lý các ngoại lệ có thể xảy ra trong quá trình xóa (vd: lỗi quyền truy cập hoặc file đang bị process khác lock). | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/audit.py:273] | Lý do: Vòng lặp trong `audit_batch` và `get_active_batch` duyệt qua `JobManager.jobs.items()` trực tiếp. Khi có nhiều request đồng thời, nếu một thread khác tạo job mới (gọi `create_job` -> `cleanup_old_jobs`), cấu trúc dictionary sẽ bị thay đổi, gây ra lỗi `RuntimeError: dictionary changed size during iteration`. | Trình bày: MUTATING_COLLECTION_ITERATION | Trọng số: -6.0
- [Maintainability] | [src/api/api_server.py:24] | Lý do: Hành vi thực tế của đoạn monkey-patch `_patched_multipart_init` là tăng `max_files` lên 10,000 (mặc định của Starlette là 1,000). Tuy nhiên, comment lại ghi là 'hạn chế DoS'. Việc tăng giới hạn này thực tế làm tăng nguy cơ cạn kiệt tài nguyên (Memory Exhaustion) nếu bị tấn công, mâu thuẫn trực tiếp với mục đích bảo mật được nêu. | Trình bày: MISLEADING_NAME | Trọng số: -1.0
- [Maintainability] | [src/config.py:55] | Lý do: Các hàm `get_ai_enabled`, `get_test_mode_limit`, và `get_auth_required` chứa các khai báo `import` bên trong body của hàm (nhằm tránh circular import nhưng vi phạm PEP8 E402). Nên chuyển các import này ra phạm vi module hoặc refactor kiến trúc database để quản lý cấu hình tập trung. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Maintainability] | [src/api/routers/members.py:53] | Lý do: Vi phạm PEP8 E402: Khai báo 'import' (from src.config import WEIGHTS) nằm bên trong hàm _aggregate_members. Mặc dù có thể được dùng để tránh circular import, nhưng việc lạm dụng này làm rối namespace và gây khó khăn cho việc quản lý dependency. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Maintainability] | [src/api/routers/repositories.py:113] | Lý do: Vi phạm PEP8 E402: Nhiều khai báo import (src.config, src.engine.ai_service) nằm rải rác bên trong các hàm handler (get_engine_settings, update_engine_settings, health_ai). Điều này vi phạm quy chuẩn cấu trúc mã nguồn Python. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Maintainability] | [src/api/routers/rules.py:42] | Lý do: Vi phạm PEP8 E402: Khai báo các scanner và compiler bên trong hàm (lines 42, 178, 203, 219, 246) thay vì ở đầu tệp. Điều này làm tăng chi phí import mỗi khi hàm được gọi thay vì chỉ một lần duy nhất lúc khởi tạo router. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Security] | [src/api/routers/history.py:20] | Lý do: Lỗ hổng Information Exposure: Sử dụng str(e) để trả về chi tiết lỗi trong HTTPException(500). Điều này có thể làm lộ thông tin nhạy cảm về cấu trúc cơ sở dữ liệu hoặc đường dẫn nội bộ của server cho người dùng cuối. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Security] | [src/api/routers/auth.py:29] | Lý do: Sử dụng JWT_SECRET_KEY mặc định ('change-me-in-production-please') nếu biến môi trường không tồn tại. Nếu dev quên cấu hình trên server thực tế, hệ thống sẽ cực kỳ dễ bị tấn công giả mạo token. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Security] | [/src/engine/natural_rules_compiler.py:98] | Lý do: Phát hiện lỗ hổng Prompt Injection nghiêm trọng. Biến 'natural_text' (đầu vào từ người dùng) được nhúng trực tiếp vào prompt hệ thống ở dòng 98 mà không có cơ chế sanitize hoặc delimited. Người dùng có thể chèn các câu lệnh điều khiển để đánh lừa AI, bỏ qua các ràng buộc bảo mật hoặc đánh cắp thông tin ngữ cảnh hệ thống (Core Rules). | Trình bày: AI_REASONING | Trọng số: -2.0
- [Maintainability] | [/src/engine/discovery.py:16] | Lý do: Vi phạm quy tắc PEP8 E402: Khai báo 'import tempfile' nằm bên trong phương thức __init__ thay vì đặt ở đầu file. Điều này làm giảm khả năng quản lý thư viện và debug namespace. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Maintainability] | [/src/engine/natural_rules_compiler.py:81] | Lý do: Vi phạm quy tắc PEP8 E402: Khai báo 'from src.api.routers.rules import _get_rules_path' nằm sâu bên trong phương thức '_get_core_rules_summary'. Trừ trường hợp đặc biệt để tránh Circular Import, các khai báo này nên được đưa lên đầu file. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Maintainability] | [/src/engine/scanners.py:354] | Lý do: Vi phạm quy tắc PEP8 E402: Khai báo 'import os' nằm bên trong phương thức '_check_calls', cụ thể là đoạn logic xử lý print statement. Cần di dời lên đầu tệp. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Security] | [/src/engine/discovery.py:27] | Lý do: Lỗ hổng Code Injection tiềm ẩn trong cơ chế sinh script tự động. Biến '{EXCLUDE_DIRS}' và '{SCAN_EXTENSIONS}' được nhúng trực tiếp vào chuỗi định dạng 'script_content' thông qua f-string. Nếu các cấu hình này có thể bị thay đổi bởi người dùng hoặc chứa các ký tự điều khiển đặc biệt, nó có thể dẫn đến việc thực thi mã Python trái phép khi subprocess chạy script này. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [/src/engine/scanners.py:150] | Lý do: Tiềm ẩn lỗi KeyError tại dòng 150 khi xử lý 'dangerous_functions'. Code truy cập trực tiếp target['name'] mà không kiểm tra xem key 'name' có tồn tại trong cấu hình luật hay không. Mặc dù có các lớp Pydantic ở compiler, nhưng scanner hoạt động độc lập và có thể nhận dữ liệu JSON thô không hợp lệ từ config. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Security] | [/src/engine/natural_rules_compiler.py:189] | Lý do: Tiếp tục phát hiện Prompt Injection trong phương thức 'auto_fix_stream'. Cả ba tham số 'failed_json', 'test_case_code', và 'error_message' đều được đưa thẳng vào prompt. Điều này cực kỳ nguy hiểm vì 'test_case_code' thường chứa mã do người dùng cung cấp hoặc kết quả từ sandbox có thể bị thao túng để chiếm quyền điều khiển AI. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Maintainability] | [src/engine/auditor.py:68] | Lý do: Biến địa phương 'id' ghi đè (shadowing) hàm built-in `id()` của Python tại dòng 68. Điều này có thể gây nhầm lẫn và lỗi logic nếu đoạn code sau đó cần sử dụng hàm định danh đối tượng nguyên bản. | Trình bày: SHADOWING_BUILTINS | Trọng số: -2.0
- [Maintainability] | [src/engine/auditor.py:122] | Lý do: Vi phạm PEP8 E402: Khai báo import thư viện (`import asyncio`, `from src.config import WEIGHTS`, ...) nằm bên trong các phương thức (`_step_ai_processing`, `_step_aggregation`, ...) thay vì ở đầu tệp. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Reliability] | [src/engine/auditor.py:538] | Lý do: Sử dụng `datetime.now()` không có thông tin múi giờ (tzinfo) để tạo báo cáo, tạo ra đối tượng naive datetime. Điều này có thể gây sai lệch thời gian khi chạy trên các máy chủ có cấu hình timezone khác nhau. | Trình bày: NAIVE_DATETIME | Trọng số: -2.0
- [Reliability] | [src/engine/auditor.py:169] | Lý do: Sử dụng `asyncio.new_event_loop()` và `run_until_complete()` bên trong một phương thức đồng bộ là một anti-pattern trong Python Asyncio. Nếu mã này được gọi trong một môi trường đã có event loop đang chạy (ví dụ web server FastAPI/Gunicorn), nó sẽ gây ra lỗi 'RuntimeError: Timeout context manager should be used inside a task' hoặc treo hệ thống. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Maintainability] | [src/engine/database.py:53] | Lý do: Vi phạm PEP8 E402: Thực hiện import bên trong hàm `initialize` (`import time`) và `seed_default_repositories` (`from src.config import CONFIGURED_REPOSITORIES`). | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Trọng số: -1.5
- [Reliability] | [src/engine/database.py:48] | Lý do: Lỗi logic xử lý kết nối: Phương thức `get_connection` gọi `initialize()` nhưng không kiểm tra xem việc khởi tạo pool thành công hay không. Nếu pool là None (do DB_URL sai/Postgres chưa sẵn sàng), lệnh `_pool.getconn()` sẽ gây crash hệ thống với lỗi AttributeError thay vì một thông báo lỗi database rõ ràng. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/engine/database.py:70] | Lý do: Cấu trúc Schema database sử dụng kiểu 'TIMESTAMP' thay vì 'TIMESTAMP WITH TIME ZONE' (timestamptz) cho các cột timestamp, created_at, updated_at. Điều này dẫn đến việc lưu trữ thời gian naive, dễ gây lỗi khi phân tích dữ liệu giữa các múi giờ khác nhau. | Trình bày: NAIVE_DATETIME | Trọng số: -2.0
- [Reliability] | [src/engine/ai_service.py:104] | Lý do: Phương thức `_call_llm_json` thực hiện catch 'Exception' chung và retry. Điều này bao gồm cả các lỗi không thể phục hồi bằng cách thử lại (như lỗi xác thực API Key, lỗi cấu trúc Pydantic), gây lãng phí tài nguyên và làm chậm phản hồi lỗi thực tế của hệ thống. | Trình bày: AI_REASONING | Trọng số: -1.0
- [Reliability] | [/src/engine/verification.py:161] | Lý do: Hàm generate_verification_script sử dụng 'inspect.getsource' để di chuyển code vào script tạm thời nhưng không sao chép các hằng số hoặc module được import ở cấp độ global của 'src.engine.scanners'. Nếu các class như BaseScanner hoặc RegexScanner phụ thuộc vào bất kỳ hằng số hoặc helper function nào không được liệt kê trong inspect, script con sẽ chết lỗi 'NameError'. [Cross-Checked: Bằng chứng hệ thống không thể tìm thấy module 'src.engine.scanners', điều này xác nhận rằng việc sử dụng 'inspect.getsource' trên các class phụ thuộc vào module này sẽ gây ra lỗi NameError tại runtime do thiếu ngữ cảnh liên kết.] | Trình bày: AI_REASONING | Trọng số: -2.0
- [Performance] | [src/api/routers/audit.py:297] | Lý do: Trong vòng lặp `background_batch`, `AuditDatabase.get_repository(pid)` được gọi để lấy thông tin repository. Ngay sau đó, hàm `run_auditor_with_capture` được gọi, bên trong lại tiếp tục gọi `AuditDatabase.get_effective_rules(target_id)` (với `target_id` chính là `pid`). Điều này gây ra các truy vấn trùng lặp vào DB cho cùng một đối tượng trong một chu kỳ xử lý ngắn mà không có caching. [Cross-Checked: Hàm 'AuditDatabase.get_effective_rules' không tồn tại trong project. Việc gọi một phương thức không tồn tại là một lỗi nghiêm trọng, xác nhận nghi vấn về sự bất ổn trong luồng xử lý và truy vấn DB.] | Trình bày: REDUNDANT_DB_QUERY | Trọng số: -4.0
- [Reliability] | [src/api/routers/audit.py:382] | Lý do: Hàm `get_fix_suggestion` thực hiện gọi `ai_service.client.chat.completions.create` mà không kiểm tra xem `ai_service.client` đã được khởi tạo thành công hay chưa. Nếu API Key thiếu hoặc cấu hình AI bị lỗi khi khởi động, dòng này sẽ gây `AttributeError: 'NoneType' object has no attribute 'chat'`. [Cross-Checked: Target 'src.engine.ai_service.ai_service' không được tìm thấy. Điều này củng cố rủi ro và xác nhận mã nguồn đang tham chiếu đến các đối tượng AI chưa được khởi tạo hoặc không tồn tại (NoneType check).] | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/config.py:58] | Lý do: Hàm `get_ai_enabled` gọi `val.lower()` sau khi kiểm tra `val is not None`. Tuy nhiên, nếu Database trả về giá trị kiểu Boolean (do mapping driver), phương thức `.lower()` sẽ không tồn tại, gây crash hệ thống (Silent Corruption/Type Error). Cần ép kiểu `str(val).lower()`. [Cross-Checked: Không tìm thấy 'AuditDatabase.get_config'. Việc thực thi .lower() trên kết quả của một hàm không xác định hoặc không tồn tại là lỗi Reliability rõ ràng, xác nhận nghi vấn ban đầu.] | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/api/routers/repositories.py:135] | Lý do: Logic bất nhất và có nguy cơ Race Condition: Hàm update_engine_settings cập nhật cấu hình vào Database nhưng các module khác vẫn sử dụng giá trị import từ 'src.config' (vốn thường là hằng số đọc từ .env lúc khởi động). Điều này khiến các thay đổi runtime không có hiệu lực thực sự trừ khi logic trong src.config cũng đọc động từ DB. [Cross-Checked: Việc không tìm thấy hàm 'AuditDatabase.set_config' chứng minh rằng cơ chế cập nhật cấu hình động đang bị hỏng hoặc tham chiếu sai, xác nhận rủi ro bất đồng bộ (Race Condition) giữa DB và runtime config.] | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/history.py:18] | Lý do: Hàm get_audit_history trả về kết quả trực tiếp từ AuditDatabase.get_history(target). Nếu Database trả về None thay vì một danh sách rỗng khi không tìm thấy, FastAPI có thể trả về Body null làm crash frontend nếu frontend mong đợi một Array. [Cross-Checked: Hệ thống không tìm thấy 'AuditDatabase.get_history'. Điều này xác nhận rằng hàm get_audit_history đang dựa trên một nguồn dữ liệu không tồn tại hoặc không ổn định, dẫn đến rủi ro trả về null thay vì array.] | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
