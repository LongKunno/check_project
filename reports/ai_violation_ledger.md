# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Maintainability] | [src/engine/ai_service.py:98] | Lý do: Function too long (God Object anti-pattern): deep_audit_batch (> 80 lines). AI Note: Hàm 'deep_audit_batch' kéo dài từ dòng 110 đến dòng 192 (khoảng 82 dòng), vượt quá ngưỡng 80 dòng được quy định. Đây là một hàm phức tạp thực hiện nhiều tác vụ như chuẩn bị prompt, gọi API với cơ chế retry, xử lý lỗi và parsing kết quả, vi phạm nguyên tắc Single Responsibility. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Maintainability] | [src/engine/ai_service.py:266] | Lý do: Function too long (God Object anti-pattern): verify_flagged_issues (> 80 lines). AI Note: Hàm 'verify_flagged_issues' kéo dài từ dòng 194 đến dòng 276 (82 dòng), vượt quá ngưỡng 80 dòng. Tương tự như 'deep_audit_batch', nó thực hiện quá nhiều logic xử lý trong một hàm duy nhất, gây khó khăn cho việc bảo trì. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Maintainability] | [src/engine/ai_service.py:189] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng 'print' trực tiếp trong mã nguồn production là một thực hành xấu. Nên sử dụng thư viện log chuẩn (ví dụ: logging) để có khả năng kiểm soát mức độ log (DEBUG, INFO, ERROR), định dạng log và log rotation. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/ai_service.py:249] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng 'print' trong khối catch lỗi của dịch vụ batch. Việc ghi log vào console qua print làm mất đi tính linh hoạt trong việc giám sát hệ thống production. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/ai_service.py:347] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng 'print' trong khối catch lỗi của cơ chế Cross-Check. Các thông tin lỗi quan trọng này cần được ghi nhận qua logger để dễ dàng truy vết và tích hợp với các hệ thống giám sát tập trung. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/scanners.py:83] | Lý do: Function too long (God Object anti-pattern): scan (> 80 lines). AI Note: Hàm `scan` trong `scanners.py` thực tế bao gồm logic xử lý cho rất nhiều loại node AST khác nhau (FunctionDef, For, While, If...), trải dài hơn 80 dòng code, vi phạm nguyên tắc về độ dài hàm để đảm bảo tính bảo trì. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Performance] | [src/engine/scanners.py:83] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): scan (Complexity: 62 > 12). AI Note: Hàm `scan` chứa một vòng lặp lớn đi qua các rule và bên trong là rất nhiều cấu trúc điều kiện lồng nhau (if/elif) để kiểm tra các loại node và quy tắc khác nhau, dẫn đến độ phức tạp vòng (Cyclomatic Complexity) vượt xa ngưỡng đề ra. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/ai_service.py:342] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Việc sử dụng lệnh print trực tiếp trong mã nguồn production (đặc biệt là trong các module thuộc engine như ai_service.py) là không tốt cho việc quản lý logs. Nên sử dụng thư viện logging để có thể cấu hình level, format và destination của log. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:42] | Lý do: Function has too many parameters (Consider DTO/Dict): log_violation (9 > 7). AI Note: Hàm log_violation có tới 9 tham số (bao gồm cả 'self'). Theo các tiêu chuẩn Clean Code (như Clean Code của Robert C. Martin), một hàm có quá 3-4 tham số đã bắt đầu khó quản lý. Việc có 9 tham số làm tăng khả năng nhầm lẫn thứ tự đối số và gây khó khăn cho việc viết Unit Test. Giải pháp dùng DTO hoặc Dictionary là hoàn toàn hợp lý. | Trình bày: TOO_MANY_PARAMS | Trọng số: -3.0
- [Maintainability] | [src/engine/auditor.py:68] | Lý do: Function too long (God Object anti-pattern): run (> 80 lines). AI Note: Việc hàm run() vượt quá 80 dòng là một dấu hiệu của 'God Function'. Trong các công cụ auditor, hàm run thường phải gánh vác nhiều việc như khởi tạo, quét file, chạy luật và tổng hợp kết quả. Nếu không được chia nhỏ, mã nguồn sẽ rất khó bảo trì và debug. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Performance] | [src/engine/auditor.py:68] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): run (Complexity: 58 > 12). AI Note: Chỉ số Cyclomatic Complexity là 58 là cực kỳ cao (ngưỡng an toàn thường là 10-15). Điều này chứng minh hàm run() chứa quá nhiều vòng lặp, câu lệnh điều kiện (if/else) lồng nhau. Mặc dù linter xếp nó vào cột Performance (vì nhiều nhánh có thể ảnh hưởng đến branch prediction của CPU), nhưng tác động chính vẫn là Maintainability. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/auditor.py:431] | Lý do: Function too long (God Object anti-pattern): generate_report (> 80 lines). AI Note: Hàm generate_report dài hơn 80 dòng thường xảy ra khi người viết trộn lẫn logic truy vấn dữ liệu và logic định dạng Markdown/HTML. Đây là True Positive vì logic tạo báo cáo nên được module hóa thành các hàm nhỏ cho từng phần (header, summary, details). | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Performance] | [src/engine/auditor.py:431] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): generate_report (Complexity: 22 > 12). AI Note: Độ phức tạp 22 mặc dù nhỏ hơn hàm run nhưng vẫn vượt xa ngưỡng khuyến nghị (12). Điều này cho thấy logic phân nhánh trong báo cáo (ví dụ: kiểm tra điểm số, xếp hạng, lặp qua các pillars) đang được xử lý quá phức tạp trong một khối code duy nhất. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Performance] | [tests/test_natural_rules_compiler.py:21] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Dựa trên ngữ cảnh 'chunk' và 'sys.stdout.flush()', đoạn mã đang thực hiện nối chuỗi trong một vòng lặp xử lý luồng dữ liệu (streaming). Trong Python, chuỗi là đối tượng bất biến (immutable), việc sử dụng '+=' trong vòng lặp tạo ra nhiều đối tượng trung gian, gây lãng phí bộ nhớ và giảm hiệu năng (O(n^2)). Nên sử dụng list.append() và join(). | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Maintainability] | [src/engine/discovery.py:117] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng lệnh `print` trong mã nguồn production bị coi là vi phạm tiêu chuẩn bảo trì. Đối với việc xuất báo cáo trong các script engine, nên sử dụng logger hoặc trả về đối tượng dữ liệu thay vì in trực tiếp ra stdout. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/api/api_server.py:15] | Lý do: Forgotten TODO/FIXME comment in code.. AI Note: Comment sử dụng tiền tố 'HACK' đánh dấu một giải pháp tạm thời/kỹ thuật không tối ưu nhưng không đi kèm mã số Issue/Ticket (như FIXME(PROJ-123)) để theo dõi. Theo chỉ đạo, đây là một ghi chú chưa được tracking và được tính là vi phạm Maintainability. | Trình bày: FORGOTTEN_TODO | Trọng số: -0.5
- [Maintainability] | [src/api/api_server.py:9] | Lý do: Import 'asyncio' might be unused (AST). AI Note: Thư viện 'asyncio' được import nhưng không được sử dụng trong đoạn mã, gây dư thừa và giảm tính bảo trì của mã nguồn (True Positive). | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Maintainability] | [src/api/api_server.py:16] | Lý do: Import 'starlette.formparsers' might be unused (AST). AI Note: Mặc dù có comment giải thích về việc tăng giới hạn upload, nhưng việc import 'starlette.formparsers' không đi kèm với bất kỳ tham chiếu sử dụng nào bên dưới để thực thi logic đó (ví dụ gán lại biến MultiPartParser.max_files). Do đó, import này là dư thừa tại thời điểm kiểm tra. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Performance] | [src/engine/dependency_checker.py:12] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): detect_circular_dependencies (Complexity: 21 > 12). AI Note: Mặc dù chỉ cung cấp phần đầu của hàm, nhưng việc phát hiện phụ thuộc vòng (circular dependencies) trong một project thường đòi hỏi các thuật toán duyệt đồ thị (như DFS) với nhiều điều kiện kiểm tra, xử lý đệ quy và quản lý trạng thái, dẫn đến Cyclomatic Complexity đạt mức 21 là hợp lý và vượt ngưỡng 12 quy định. Đây là một vi phạm thật về thiết kế code. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Performance] | [src/engine/verification.py:78] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): load_rules (Complexity: 15 > 12). AI Note: Chỉ số Cyclomatic Complexity (15) vượt quá giới hạn khuyến nghị (12). Hàm load_rules thực hiện việc đọc và gộp các quy tắc, thường chứa nhiều logic lặp và điều kiện, dẫn đến khó khăn trong việc bảo trì và kiểm thử. Đây là lỗi thật cần được cải thiện bằng cách chia nhỏ hàm. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/verification.py:28] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng lệnh print trực tiếp trong mã nguồn engine là lỗi thật. Trong các thành phần lõi của hệ thống, nên sử dụng thư viện logging (logger.error) để cho phép kiểm soát định dạng log, mức độ nghiêm trọng và khả năng thu thập log tập trung. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/verification.py:55] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Tương tự như vi phạm #1, việc in thông báo lỗi bằng print khi quét file không phù hợp với chuẩn mã nguồn production. Việc thay thế bằng logging sẽ giúp theo dõi hệ thống tốt hơn mà không gây nhiễu luồng đầu ra tiêu chuẩn. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/verification.py:197] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Mặc dù chuỗi in ra có chứa emoji gợi ý đây có thể là một script CLI, nhưng vì nó nằm trong module engine/verification.py, việc sử dụng print vẫn được coi là vi phạm chuẩn thiết kế. Nên raise exception hoặc log lại để lớp giao diện (CLI) xử lý việc hiển thị. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Performance] | [src/api/git_helper.py:12] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): clone_repository (Complexity: 13 > 12). AI Note: Chỉ số độ phức tạp Cyclomatic (13) vượt mức giới hạn cho phép (12). Đối với các hàm xử lý Git clone có nhiều tham số tùy chọn như username, token, branch và kiểm tra thư mục, số lượng các nhánh rẽ điều kiện (if/else) và xử lý ngoại lệ thường cao, nên báo cáo về độ phức tạp là chính xác. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/api/git_helper.py:3] | Lý do: Import 'urllib.parse' might be unused (AST). AI Note: Báo lỗi dựa trên phân tích AST (Abstract Syntax Tree) cho thấy module 'urllib.parse' được import nhưng không được tham chiếu trong mã nguồn. Đây là một vi phạm về Maintainability (Khả năng bảo trì) thực tế (True Positive). | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Performance] | [src/api/routers/audit.py:92] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): upload_and_audit (Complexity: 14 > 12). AI Note: Độ phức tạp đạt mức 14 (vượt ngưỡng 12). Các hàm API điều phối nhiều tác vụ (nhận file, lưu tạm, gọi background task) thường có nhiều luồng logic rẽ nhánh, do đó vi phạm này phản ánh đúng trạng thái của mã nguồn hiện tại. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Reliability] | [src/engine/auditor.py:89] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Khối ngoại lệ bắt 'ImportError' nhưng chỉ chứa lệnh 'pass' mà không thực hiện ghi log (logging) hay xử lý lại lỗi. Điều này khiến lỗi bị nuốt hoàn toàn, gây khó khăn cho việc gỡ lỗi khi hệ thống thiếu phụ thuộc (dependency) cần thiết. Dựa trên trụ cột Reliability, việc im lặng trước một lỗi tiềm ẩn là một lỗi thật (True Positive). | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Reliability] | [src/config.py:31] | Lý do: Biến môi trường 'TEST_MODE_LIMIT_FILES' được ép kiểu int trực tiếp mà không có bọc try/except. Nếu biến này được cấu hình sai (ví dụ: chuỗi trống hoặc ký tự không phải số), ứng dụng sẽ crash ngay khi khởi động (Fatal Startup Error). | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Maintainability] | [src/api/audit_state.py:68] | Lý do: Class 'AuditState' (đang được AuditLogHandler sử dụng) lưu trữ log vào một list static dùng chung 'legacy_logs'. Trong môi trường API đa người dùng (FastAPI), việc nhiều jobs chạy song song sẽ khiến log của JOB_A bị ghi đè hoặc trộn lẫn với JOB_B. Ngoài ra, hàm 'reset()' sẽ xóa log của tất cả các phiên đang chạy. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [src/api/git_helper.py:52] | Lý do: Cơ chế 'clone_repository' sử dụng 'shutil.rmtree(dest_dir)' khi clone shallow thất bại. Nếu tham số 'dest_dir' do người dùng kiểm soát hoặc bị cấu hình sai trỏ vào các thư mục hệ thống quan trọng, nó có thể gây mất dữ liệu nghiêm trọng trước khi thử fallback clone. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Performance] | [src/api/audit_state.py:20] | Lý do: JobManager lưu trữ log và trạng thái job trong dict/list static mà không có cơ chế dọn dẹp (eviction/TTL). Theo thời gian, việc thực hiện nhiều ca scan mã nguồn dung lượng lớn sẽ gây ra rò rỉ bộ nhớ (Memory Leak) phía Server. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [tests/test_natural_rules_compiler.py:34] | Lý do: Đoạn code ép buộc JSON bằng cách thêm dấu ngoặc nhọn '{' + json_str + '}' nếu không bắt đầu bằng '{'. Đây là xử lý dữ liệu đầu vào từ AI (external source) một cách cảm tính. Nếu 'json_str' là một mảng hoặc dữ liệu không hoàn chỉnh, việc ép kiểu này có thể tạo ra JSON hợp lệ về cú pháp nhưng sai lệch về ngữ nghĩa (Silent Corruption), dẫn đến việc lưu các rule sai vào DB. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Security] | [src/engine/discovery.py:15] | Lý do: Vulnerabilidade de Code Injection: O método 'generate_precheck_script' interpola variáveis (EXCLUDE_DIRS, SCAN_EXTENSIONS) diretamente em uma string de script Python que é executada via subprocess. Se um usuário puder manipular essas configurações (ex: via custom_rules ou env vars), ele pode executar código arbitrário no host. | Trình bày: AI_REASONING | Trọng số: -9.0
- [Reliability] | [src/engine/auditor.py:70] | Lý do: Uso inseguro do resultado de 'run_discovery'. O código assume que 'self.discovery_data' sempre terá a chave 'files' e 'total_loc'. Se o subprocesso em DiscoveryStep falhar ou o JSON estiver malformado, o Auditor irá crashar com KeyError ou TypeError. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/engine/database.py:105] | Lý do: Possível corrupção de fluxo/crash: As chamadas para 'json.loads(d.get("full_json"))' ocorrem sem blocos try/except. Se dados inválidos/truncados forem persistidos no PostgreSQL, a recuperação do histórico ou detalhes de uma auditoria falhará completamente, impedindo o acesso aos resultados. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/engine/auditor.py:137] | Lý do: Risco de Out-of-Memory (OOM): O código lê o conteúdo integral de arquivos para memória ('content = file_obj.read()') antes de realizar o cálculo de tamanho para o smart batching. Em repositórios com minified JS ou arquivos de dados grandes acidentalmente incluídos, isso causará crash no worker de auditoria. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Performance] | [src/engine/auditor.py:135] | Lý do: I/O Redundante e Ineficiente: Os arquivos são lidos integralmente em DiscoveryStep (via readlines), novamente em VerificationStep (implícito) e uma terceira vez no Auditor.run (passo 3.6). Para projetos grandes, a sobrecarga de I/O em disco reduz significativamente a performance. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [src/engine/dependency_checker.py:49] | Lý do: Potencial 'RecursionError' no algoritmo DFS. O detector de dependências circulares não impõe um limite de profundidade e não utiliza uma abordagem iterativa. Em arquiteturas de larga escala com centenas de módulos interconectados, o limite de recursão do Python pode ser atingido. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/engine/auditor.py:108] | Lý do: Gerenciamento de Event Loop inadequado: O uso repetido de 'asyncio.get_event_loop()' e 'loop.run_until_complete()' dentro de uma classe que deveria ser puramente lógica pode causar conflitos em ambientes onde o loop já está rodando (como FastAPI/Uvicorn), levando a RuntimeError: 'This event loop is already running'. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [src/engine/auditor.py:193] | Lý do: Falha na verificação de tipos em dados dinâmicos: Ao processar resultados da AI ('rv.get("weight")'), o código faz um cast direto para float. Se a AI retornar um valor não numérico, o processo de agregação falhará totalmente no meio da execução. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/engine/verification.py:110] | Lý do: Đoạn mã thực hiện ép kiểu `float(custom_weights[r_id])` trực tiếp trên dữ liệu cấu hình từ người dùng (`custom_rules`) mà không có khối try/except. Nếu người dùng nhập một giá trị trọng số không phải số (ví dụ: 'abc'), hệ thống sẽ crash hoàn toàn trong quá trình load rules. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/engine/natural_rules_compiler.py:74] | Lý do: Trong vòng lặp xử lý stream từ OpenAI/AI service, code truy cập trực tiếp `chunk.choices[0]` mà không kiểm tra xem mảng `choices` có rỗng hay không. Trong một số trường hợp (như khi stream kết thúc hoặc bị filter content), API có thể trả về mảng rỗng, dẫn đến lỗi 'Index out of range' làm gián đoạn quá trình biên dịch luật. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/engine/verification.py:172] | Lý do: Hàm `run_verification` lấy dòng cuối cùng của stdout (`stdout_lines[-1]`) để parse JSON. Nếu script verify gặp lỗi runtime trước khi in kết quả hoặc in thêm logs debug, `stdout_lines` có thể rỗng (gây IndexError) hoặc chứa dữ liệu rác làm `json.loads` thất bại. Việc tin tưởng tuyệt đối vào dòng cuối cùng của stdout từ một tiến trình con là không an toàn. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Maintainability] | [src/engine/scoring.py:15] | Lý do: Tên gọi 'Laplace Smoothing' được dùng trong comment và logic gán `effective_loc = max(total_loc, 500)` là sai bản chất kỹ thuật. Laplace Smoothing là kỹ thuật cộng thêm tham số vào phân phối xác suất, trong khi ở đây chỉ là gán giá trị sàn (Floor Threshold). Điều này gây hiểu lầm cho người bảo trì về phương pháp tính toán trọng số thực tế. | Trình bày: MISLEADING_NAME | Trọng số: -3.0
- [Performance] | [src/engine/scanners.py:43] | Lý do: Trong `RegexScanner`, lệnh `content[:start_idx].count('\n') + 1` được gọi lại cho mỗi vi phạm tìm thấy. Đối với file lớn (nhiều MB) và nhiều lỗi, thao tác copy string và đếm dòng này có độ phức tạp O(N*M) (N: độ dài file, M: số vi phạm), gây chậm trễ nghiêm trọng trong quá trình quét. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Maintainability] | [src/engine/scanners.py:128] | Lý do: Hàm `scan` của `PythonASTScanner` trực tiếp sửa đổi đối tượng AST bằng cách gán `child.parent = parent_node`. Các đối tượng AST node là shared state trong quá trình duyệt cây; việc thay đổi cấu trúc của chúng có thể gây ra side-effect không lường trước được cho các scanner khác chạy sau hoặc các pass phân tích khác. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/audit.py:32] | Lý do: Hàm 'run_auditor_with_capture' thực hiện ghi đè toàn cục 'sys.stdout'. Trong môi trường web bất đồng bộ như FastAPI, khi có nhiều yêu cầu audit diễn ra hoặc các tiến trình khác ghi log, luồng dữ liệu sẽ bị tranh chấp (race condition). Điều này dẫn đến việc log của các request bị trộn lẫn vào nhau hoặc bị mất, và 'sys.stdout' có thể không được khôi phục đúng cách nếu xảy ra lỗi nghiêm trọng giữa chừng. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Maintainability] | [src/api/routers/audit.py:95] | Lý do: Lỗi logic trong vòng lặp xử lý file: Biến 'project_name' bị ghi đè liên tục bởi 'safe_parts[0]' của mỗi file trong danh sách upload. Nếu một gói upload chứa nhiều thư mục khác nhau, 'project_name' sẽ chỉ nhận giá trị của thư mục thuộc file cuối cùng. Điều này dẫn đến việc khởi tạo JobID và TargetPath không nhất quán, có thể gây lỗi lưu trữ kết quả audit. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Performance] | [src/engine/ai_service.py:84] | Lý do: Hàm '_load_ai_only_rules' thực hiện đọc file 'rules.json' từ disk trên mỗi lần gọi 'deep_audit_batch'. Với dự án lớn gồm nhiều chunk file, việc lặp lại thao tác I/O này là cực kỳ chậm và lãng phí tài nguyên. Quy tắc (rules) nên được tải vào bộ nhớ một lần duy nhất (static/singleton) khi ứng dụng khởi chạy. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/api/routers/audit.py:78] | Lý do: Chức năng 'cancel_audit' chỉ thay đổi trạng thái flag 'AuditState.is_running' nhưng code thực thi chính ('auditor.run()') lại chạy trong 'asyncio.to_thread' hoặc background task mà không hề có cơ chế kiểm tra flag này để dừng lại. Đây là một tên gọi gây hiểu lầm vì nó không thực sự 'hủy' tiến trình đang chạy mà chỉ đánh lừa UI, gây lãng phí tài nguyên server và AI tokens. | Trình bày: MISLEADING_NAME | Trọng số: -4.0
- [Reliability] | [src/api/routers/audit.py:39] | Lý do: Hệ thống sử dụng 'AuditState.logs' (một list toàn cục) để lưu trữ log của các phiên audit. Danh sách này được thêm mới liên tục ở 'AuditLogStream.write' nhưng không bao giờ được dọn dẹp hoặc giới hạn kích thước (bounding). Điều này sẽ dẫn đến việc tiêu tốn bộ nhớ RAM ngày càng tăng (Memory Leak) khi server chạy lâu dài với nhiều phiên audit. | Trình bày: AI_REASONING | Trọng số: -4.5
- [Performance] | [src/api/routers/audit.py:105] | Lý do: Trong hàm 'upload_and_audit', lệnh 'await upload_file.read()' đọc toàn bộ nội dung file vào bộ nhớ RAM. Nếu người dùng upload các file mã nguồn cực lớn hoặc file binary giả mạo, server có thể bị tấn công Denial of Service (DoS) do cạn kiệt bộ nhớ (OOM - Out of Memory). Nên sử dụng streaming buffer để lưu file vào disk. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -3.5
- [Reliability] | [src/api/api_server.py:63] | Lý do: Hàm startup_event gọi 'AuditDatabase.initialize()' nhưng chỉ bắt Exception và log error. Nếu DB initialization thất bại, ứng dụng vẫn tiếp tục chạy ở trạng thái lỗi (zombie state), dẫn đến crash hàng loạt khi có request truy cập DB ở các routers. [Cross-Checked: Việc bắt mọi Exception và chỉ log lỗi khi khởi tạo Database là một lỗi thiết kế Reliability nghiêm trọng (Zombie process). Dù hệ thống AST không tìm thấy định nghĩa của 'initialize', logic tại nơi gọi (api_server.py) được mô tả là cho phép ứng dụng tiếp tục chạy mặc dù thành phần cốt lõi thất bại.] | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [tests/test_natural_rules_compiler.py:43] | Lý do: Biến 'res' nhận kết quả từ 'AuditDatabase.get_project_rules('test_target')' và ngay sau đó được truy cập các key như 'res['natural_text']' mà không kiểm tra None. Trong các hệ quản trị database, việc không tìm thấy bản ghi thường trả về None. Nếu 'test_target' không tồn tại (ví dụ do lỗi DB hoặc race condition), code sẽ crash với TypeError thay vì xử lý lỗi một cách an toàn. [Cross-Checked: Trong Python, truy cập 'res['natural_text']' mà không kiểm tra 'res is not None' là rủi ro gây TypeError. Mặc dù ở trong file test, việc để test crash do lỗi logic thay vì lỗi assertion vẫn được coi là thiếu tin cậy (Unreliable test logic).] | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [tests/test_authorship.py:43] | Lý do: Hàm 'tracker.get_author_info' trả về thông tin tác giả cho một dòng cụ thể. Kết quả 'info1' được sử dụng trực tiếp 'info1['boundary']' mà không qua kiểm tra null-check. Nếu file 'main.py' không được git tracker nhận diện hoặc số dòng vượt quá giới hạn, hàm này có khả năng trả về None, dẫn đến lỗi runtime AttributeError/TypeError trong unit test. [Cross-Checked: Tương tự cờ #1, 'get_author_info' có khả năng trả về None (ví dụ: file không tồn tại trong Git index). Việc truy cập 'info1['boundary']' trực tiếp mà không có null-check là một lỗi lập trình thiếu an toàn.] | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [tests/test_discovery.py:52] | Lý do: Tương tự, 'report1 = discovery1.run_discovery()' và 'report2 = discovery2.run_discovery()' trả về một báo cáo được truy cập trực tiếp qua key ['features']. Nếu quá trình discovery thất bại hoặc thư mục không hợp lệ dẫn đến trả về None, code sẽ crash. Việc thiếu null-check cho các hàm engine chính là một rủi ro Reliability lớn. [Cross-Checked: Kết quả từ các engine discovery thường phụ thuộc vào dữ liệu ngoại vi. Việc truy cập key ['features'] mà không kiểm tra tính hợp lệ của report1/report2 là rủi ro crash runtime.] | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/engine/verification.py:139] | Lý do: Cơ chế `generate_verification_script` sử dụng `inspect.getsource` để sao chép code vào một script tạm. Điều này rất dễ lỗi nếu các hàm được copy (như `detect_circular_dependencies`) phụ thuộc vào các biến global, các import nội bộ khác trong file gốc, hoặc nếu môi trường chạy subprocess python3 không có quyền truy cập vào các module như `src.config`. [Cross-Checked: Bằng chứng mã nguồn xác nhận hàm 'detect_circular_dependencies' phụ thuộc vào hàm hỗ trợ '_build_flat_meta' và các module 'os', 'ast', 'logging' ở phạm vi global. Việc dùng 'inspect.getsource' để chạy độc lập chắc chắn sẽ gây lỗi NameError vì thiếu các định nghĩa này.] | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [src/engine/scoring.py:40] | Lý do: Lệnh `from src.config import WEIGHTS` nằm bên trong phương thức tĩnh. Nếu file này được thực thi trong môi trường tách biệt của `VerificationStep` (nơi PYTHONPATH có thể không bao gồm `src`), nó sẽ gây ra `ModuleNotFoundError`. Cần kiểm tra xem file `src/config.py` có tồn tại và cấu trúc `WEIGHTS` có khớp không. [Cross-Checked: Bằng chứng 'src.config not found' củng cố nghi vấn rằng cấu trúc package 'src' không phải lúc nào cũng được giải quyết ổn định. Việc import tuyệt đối bên trong phương thức sẽ gây lỗi nếu môi trường thực thi (VerificationStep) không có cấu hình PYTHONPATH chính xác.] | Trình bày: AI_REASONING | Trọng số: -4.0
