# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Reliability] | [src/engine/database.py:34] | Lý do: Sử dụng 'psycopg2.pool.SimpleConnectionPool' trong một kiến trúc có sử dụng Asyncio/Concurrency. Theo tài liệu của psycopg2, SimpleConnectionPool không thread-safe. Trong môi trường kiểm toán mã nguồn quy mô lớn hoặc chạy tích hợp trong web server, điều này có thể dẫn đến lỗi tranh chấp kết nối (connection race conditions) hoặc hỏng trạng thái pool nội bộ. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Reliability] | [src/engine/dependency_checker.py:32] | Lý do: Lỗi logic ánh xạ module (Short Name Collision). Hàm '_build_mappings' sử dụng tên file ngắn (basename) làm key trong 'mod_to_f'. Trong các dự án Python lớn, các file như 'models.py', 'utils.py' thường xuất hiện ở nhiều package khác nhau (vd: app1/models.py và app2/models.py). Việc chỉ lưu 'short_name' sẽ khiến ánh xạ sau ghi đè ánh xạ trước, dẫn đến việc phát hiện Circular Dependency sai lệch hoàn toàn hoặc bỏ sót các mối liên kết thực tế. | Trình bày: AI_REASONING | Trọng số: -6.0
- [Security] | [src/engine/discovery.py:105] | Lý do: Cơ chế 'Code Injection by Design'. Hàm 'run_discovery' thực hiện ghi một kịch bản Python mới ('ai_precheck.py') vào thư mục đích (target_dir) và thực thi nó bằng 'subprocess.run'. Nếu người dùng có quyền ghi vào thư mục này hoặc nếu 'target_dir' là một repo không tin cậy, điều này cho phép thực thi mã tùy ý trong môi trường của Auditor. Ngoài ra, việc thực thi python3 mà không thông qua venv hiện tại có thể dẫn đến sai lệch library. | Trình bày: AI_REASONING | Trọng số: -8.0
- [Reliability] | [src/engine/database.py:218] | Lý do: Race condition trong logic 'toggle_core_rule'. Mặc dù có sử dụng 'SELECT ... FOR UPDATE', nhưng nếu 'target_id' chưa tồn tại, lock sẽ không được thiết lập trên bản ghi. Nếu hai luồng cùng gọi hàm này đồng thời cho một 'target_id' mới, cả hai đều thấy 'row' là None và cùng thực hiện 'INSERT', dẫn đến lỗi 'Unique Violation' (IntegrityError) cho cột target_id_unique. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/engine/auditor.py:139] | Lý do: Quản lý Event Loop không an toàn. Hàm '_step_ai_validation' và '_step_ai_reasoning' tạo mới event loop bằng 'asyncio.new_event_loop()' và chạy 'run_until_complete'. Nếu CodeAuditor được tích hợp vào một ứng dụng đã có sẵn loop (như Fast API hoặc ứng dụng Async khác), việc này sẽ gây lỗi 'RuntimeError: Timeout context manager should be used inside a task' hoặc xung đột loop hiện hữu. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Maintainability] | [tests/test_overall_pillars.py:67] | Lý do: Có sự mâu thuẫn nghiêm trọng giữa tài liệu (comment dòng 64) và kỳ vọng thực tế (assert dòng 67). Comment tính toán Score = 1.67 dựa trên logic phạt, nhưng assert lại mong đợi giá trị 8.18. Điều này cho thấy nomenclature 'project_pillars' hoặc cách tính toán engine đang gây hiểu lầm cho người đọc mã nguồn về bản chất của điểm số (là điểm cộng hay điểm trừ/phạt). | Trình bày: MISLEADING_NAME | Trọng số: -1.0
- [Maintainability] | [tests/test_natural_rules_compiler.py:30] | Lý do: Logic sửa lỗi định dạng JSON (`if not json_str.startswith('{'): json_str = '{' + json_str + '}'`) quá thô sơ. Nếu AI trả về một danh sách JSON (JSON Array `[...]`), việc cố tình chèn thêm dấu ngoặc nhọn `{` ở đầu sẽ tạo ra một chuỗi JSON không hợp lệ, thay vì sửa chữa nó. Điều này gây ra lỗi parsing không đáng có. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [tests/test_authorship.py:10] | Lý do: Việc sử dụng hardcode thư mục tạm `/tmp/test_authorship_project` mà không có cơ chế random hóa tên hoặc cleanup an toàn (race condition) khiến test case dễ bị lỗi nếu chạy song song (parallel testing) hoặc chạy trên môi trường có nhiều user. Điều này làm giảm tính cô lập của test suite. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/audit.py:48] | Lý do: Biến toàn cục `AuditState.is_running` gây ra Race Condition trong môi trường đa nhiệm. Khi nhiều Job chạy song song, hàm `run_auditor_with_capture` đặt `is_running = False` trong khối `finally`. Nếu Job A kết thúc trong khi Job B vẫn đang chạy, trạng thái hệ thống sẽ bị báo sai là 'Idle' (False), gây ảnh hưởng đến UI/UX và logic điều khiển. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Reliability] | [src/api/routers/audit.py:113] | Lý do: Hàm `AuditState.reset()` xóa sạch danh sách log toàn cục (`AuditState.logs`) mỗi khi một cuộc kiểm toán mới bắt đầu. Trong mô hình FastAPI với BackgroundTasks, nếu User A đang xem log của Job A qua SSE (`/audit/logs`), và User B bắt đầu một Job mới, toàn bộ lịch sử log của User A sẽ bị xóa khỏi stream, gây mất dữ liệu hiển thị thời gian thực. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [src/api/routers/audit.py:115] | Lý do: Tên project được fix cứng là `"uploaded_project"` trong endpoint `/audit/process`. Khi nhiều người dùng upload code cùng lúc, `JobManager.create_job(project_name)` sẽ gây xung đột ID hoặc ghi đè trạng thái Job của nhau nếu JobManager sử dụng tên làm khóa định danh. | Trình bày: AI_REASONING | Trọng số: -6.0
- [Reliability] | [src/api/routers/audit.py:249] | Lý do: Sử dụng kết quả trả về từ AI mà không kiểm tra tính tồn tại của các nhánh response. Nếu API trả về danh sách `choices` rỗng (do filter content hoặc lỗi model), mã sẽ gây ra `IndexError: list index out of range`. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/api/routers/audit.py:53] | Lý do: Truy cập trực tiếp vào `auditor.discovery_data['total_loc']` và các key khác mà không kiểm tra sự tồn tại. Nếu tiến trình kiểm toán bị lỗi giữa chừng hoặc `auditor.run()` không khởi tạo đầy đủ dữ liệu discovery, hàm này sẽ ném ra `KeyError` và làm hỏng logic lưu kết quả. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Maintainability] | [src/engine/ai_service.py:100] | Lý do: Phương thức `_load_ai_only_rules` phụ thuộc vào đường dẫn tương đối cứng nhắc (`__file__`) để tìm `rules.json`. Điều này có thể gây lỗi khi chạy ứng dụng từ các entrypoint khác nhau hoặc khi đóng gói package (ví dụ: dùng PyInstaller). Nên sử dụng cấu hình đường dẫn từ `src.config` hoặc inject registry. | Trình bày: AI_REASONING | Trọng số: -1.0
- [Maintainability] | [src/engine/verification.py:105] | Lý do: Lỗi logic trong '_merge_custom_payload': Mã nguồn kiểm tra 'isinstance(custom.get('ast_rules'), list)' để xử lý luật AST cũ. Tuy nhiên, Pydantic Schema trong 'natural_rules_compiler.py' và logic sinh luật AI định nghĩa 'ast_rules' là một Dictionary (mapping dangerous_functions). Điều này khiến các luật AST do AI sinh ra bị bỏ qua hoàn toàn do sai kiểu dữ liệu. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [src/engine/scanners.py:169] | Lý do: Hàm '_check_calls' khi kiểm tra '__main__' guard truy cập trực tiếp vào 'test.comparators[0].value'. Nếu biểu thức so sánh không phải là hằng số (ví dụ: 'if __name__ == some_variable:'), 'comparators[0]' có thể là một node 'ast.Name' không có thuộc tính 'value', gây ra lỗi 'AttributeError' và làm dừng quá trình quét. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/engine/natural_rules_compiler.py:72] | Lý do: Truy cập trực tiếp 'chunk.choices[0]' trong luồng streaming mà không kiểm tra độ dài của mảng 'choices'. Trong trường hợp API trả về lỗi hoặc bị chặn bởi bộ lọc nội dung (Content Filter) khiến 'choices' rỗng, mã nguồn sẽ gây ra 'IndexError'. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -3.0
- [Performance] | [src/engine/scanners.py:158] | Lý do: Sử dụng 'import os' và tính toán 'os.path.basename(file_path)' lặp đi lặp lại bên trong vòng lặp 'walk' của AST mỗi khi gặp hàm 'print'. Việc import và tính toán string không cần thiết trong loop core khiến hiệu năng quét giảm đáng kể trên các dự án lớn. | Trình bày: AI_REASONING | Trọng số: -1.0
- [Performance] | [src/api/api_server.py:21] | Lý do: Vá lỗi (Monkey-patch) Starlette MultiPartParser tăng giới hạn max_files và max_fields lên 10,000 nhưng chú thích lại ghi là 'hạn chế DoS'. Thực tế, việc tăng mạnh giới hạn này (mặc định của Starlette là 1000) làm tăng nguy cơ bị tấn công Từ chối dịch vụ (DoS) thông qua cạn kiệt tài nguyên bộ nhớ khi xử lý các request multipart/form-data quá lớn. | Trình bày: MISLEADING_NAME | Trọng số: -3.0
- [Reliability] | [src/api/audit_state.py:26] | Lý do: Lớp JobManager lưu trữ toàn bộ log của mỗi job dưới dạng một mảng (List[str]). Không có cơ chế giới hạn kích thước mảng hoặc dung lượng mỗi dòng log. Đối với các dự án lớn có hàng nghìn tệp và hàng triệu dòng mã, quá trình kiểm toán có thể tạo ra khối lượng log khổng lồ, dẫn đến lỗi tràn bộ nhớ (Out of Memory - OOM) và làm sập API server. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Security] | [src/api/git_helper.py:78] | Lý do: Biểu thức chính quy (Regex) dùng để ẩn Token trong lỗi Git (`(?<=://)[^@]+(?=@)`) có thể không lọc sạch thông tin nhạy cảm nếu cấu trúc thông báo lỗi của Git thay đổi (ví dụ: lỗi trong stderr hiển thị trực tiếp token mà không nằm trong URL). Việc lộ lọt Bitbucket Token ra log hệ thống là một rủi ro bảo mật nghiêm trọng. | Trình bày: AI_REASONING | Trọng số: -6.0
- [Maintainability] | [src/api/audit_state.py:100] | Lý do: Lớp legacy 'AuditState' sử dụng một mảng tĩnh `logs = []` chung cho toàn hệ thống. Trong kiến trúc web đa luồng (concurrent requests), các log không thể xác định được job_id sẽ bị trộn lẫn vào nhau trong mảng này, vi phạm nguyên tắc bao đóng dữ liệu và gây nhầm lẫn khi người dùng xem luồng log chung. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [src/engine/verification.py:134] | Lý do: Phương thức 'generate_verification_script' sử dụng 'inspect.getsource' để trích xuất mã nguồn của hàm 'detect_circular_dependencies' và các Scanner. Tuy nhiên, nếu 'detect_circular_dependencies' phụ thuộc vào các hàm helper hoặc biến toàn cục khác trong file 'src/engine/dependency_checker.py' (không được inspect), script tạo ra sẽ bị lỗi 'NameError' khi chạy trong tiến trình con (subprocess). [Cross-Checked: Bằng chứng mã nguồn xác nhận hàm `detect_circular_dependencies` sử dụng các phụ thuộc ngoài phạm vi (global scope) bao gồm hàm `_build_flat_meta` và các module `os`, `ast`. Vì `inspect.getsource` chỉ trích xuất mã nguồn của chính hàm đó, script chạy trong subprocess sẽ thiếu các định nghĩa này, dẫn đến lỗi NameError chắc chắn xảy ra.] | Trình bày: AI_REASONING | Trọng số: -5.0
