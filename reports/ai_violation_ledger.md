# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Performance] | [src/engine/auditor.py:69] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): run (Complexity: 58 > 12). AI Note: Hàm `run` có độ phức tạp Cyclomatic là 58, vượt xa ngưỡng 12. Điều này là do hàm chứa quá nhiều vòng lặp lồng nhau và các nhánh điều kiện (if/else) để xử lý toàn bộ quy trình kiểm toán từ quét file, xử lý rules, đến lưu log. Đây là một True Positive rõ ràng về mặt hiệu năng và khả năng bảo trì. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/auditor.py:432] | Lý do: Function too long (God Object anti-pattern): generate_report (> 80 lines). AI Note: Hàm `generate_report` dài hơn 80 dòng code. Nó thực hiện quá nhiều nhiệm vụ: tính toán điểm số, định dạng Markdown cho từng trụ cột, liệt kê chi tiết lỗi và ghi file. Việc gộp tất cả vào một hàm vi phạm nguyên tắc Single Responsibility, xác nhận là True Positive. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Performance] | [src/engine/auditor.py:432] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): generate_report (Complexity: 22 > 12). AI Note: Độ phức tạp 22 vượt ngưỡng 12 do hàm phải lặp qua nhiều danh sách kết quả, phân loại theo Pillar và Feature, đồng thời xử lý các định dạng hiển thị khác nhau. Cần được tách nhỏ để giảm độ phức tạp. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Reliability] | [src/engine/auditor.py:90] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Khối `except ImportError: pass` nuốt lỗi mà không có bất kỳ thông báo hay ghi log nào. Mặc dù có thể là có ý đồ bỏ qua khi thiếu module tùy chọn, nhưng việc không log lại khiến việc gỡ lỗi trở nên khó khăn nếu module đó thực sự cần thiết cho luồng xử lý. Đây là True Positive về Reliability. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Performance] | [src/engine/dependency_checker.py:12] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): detect_circular_dependencies (Complexity: 21 > 12). AI Note: Hàm `detect_circular_dependencies` chứa logic đệ quy hoặc các vòng lặp duyệt đồ thị phức tạp để phát hiện vòng lặp phụ thuộc. Với độ phức tạp 21, nó gây khó khăn cho việc hiểu và kiểm thử logic phát hiện lỗi, xác nhận là True Positive. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/api/api_server.py:15] | Lý do: Forgotten TODO/FIXME comment in code.. AI Note: Comment sử dụng từ khóa 'HACK', đây là một dạng ghi chú về giải pháp tạm thời tương tự như TODO/FIXME. Do không có mã số Ticket hoặc Issue đi kèm để theo dõi (tracking), nó được coi là một ghi chú lãng quên/tạm thời trong mã nguồn theo chỉ đạo. | Trình bày: FORGOTTEN_TODO | Trọng số: -0.5
- [Maintainability] | [src/api/api_server.py:9] | Lý do: Import 'asyncio' might be unused (AST). AI Note: Thư viện 'asyncio' được import nhưng không thấy được sử dụng trong các đoạn mã thực thi được cung cấp (như uvicorn.run). Đây là một lỗi import thừa (Unused import). | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Performance] | [src/api/git_helper.py:12] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): clone_repository (Complexity: 13 > 12). AI Note: Chỉ số độ phức tạp Cyclomatic (13) đã vượt quá ngưỡng cho phép (12). Đây là vi phạm thực tế về khả năng bảo trì và độ phức tạp của mã nguồn. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Performance] | [src/api/routers/audit.py:108] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): upload_and_audit (Complexity: 14 > 12). AI Note: Hàm xử lý file upload tích hợp với Background Tasks và trả về response thường chứa nhiều khối try-except, vòng lặp qua danh sách file và kiểm tra điều kiện (if/else), dẫn đến độ phức tạp đạt 14 là hoàn toàn khả thi và vi phạm ngưỡng 12. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/ai_service.py:101] | Lý do: Function too long (God Object anti-pattern): deep_audit_batch (> 80 lines). AI Note: Các hàm tương tác với AI thường bao gồm các bước: chuẩn bị chunk, xây dựng prompt, gọi API, xử lý lỗi retry và parse kết quả JSON. Việc vượt quá 80 dòng là lỗi phổ biến nếu không tách nhỏ service. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Maintainability] | [src/engine/ai_service.py:279] | Lý do: Function too long (God Object anti-pattern): verify_flagged_issues (> 80 lines). AI Note: Quy trình xác minh (cross-check) yêu cầu đối chiếu dữ liệu giữa cache và kết quả mới, thường đi kèm logic xử lý mảng phức tạp, dễ dàng vượt ngưỡng 80 dòng code. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Maintainability] | [src/engine/auditor.py:43] | Lý do: Function has too many parameters (Consider DTO/Dict): log_violation (9 > 7). AI Note: Hàm log_violation có tới 8 tham số (không tính self), vượt quá ngưỡng khuyến nghị là 7. Việc gom nhóm các thuộc tính này vào một đối tượng Violation (DTO) hoặc Metadata Dict là cần thiết để cải thiện tính bảo trì. | Trình bày: TOO_MANY_PARAMS | Trọng số: -3.0
- [Maintainability] | [src/engine/auditor.py:69] | Lý do: Function too long (God Object anti-pattern): run (> 80 lines). AI Note: Hàm run() đóng vai trò điều phối (orchestrator) thường có xu hướng phình to vì phải gọi tuần tự nhiều giai đoạn của hệ thống. Nếu vượt quá 80 dòng, đây là dấu hiệu của việc chưa模块 hóa tốt. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Maintainability] | [src/engine/scanners.py:83] | Lý do: Function too long (God Object anti-pattern): scan (> 80 lines). AI Note: Việc hàm scan vượt quá 80 dòng là một vi phạm về khả năng bảo trì (Maintainability). Hàm quá dài thường gây khó khăn cho việc đọc hiểu, kiểm thử đơn vị và dễ phát sinh lỗi khi thay đổi logic. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Performance] | [src/engine/scanners.py:83] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): scan (Complexity: 62 > 12). AI Note: Độ phức tạp Cyclomatic lên tới 62 là cực kỳ cao so với ngưỡng khuyến nghị (thường là 10-12). Điều này cho thấy hàm chứa quá nhiều điều kiện rẽ nhánh và vòng lặp lồng nhau, vi phạm nghiêm trọng nguyên tắc cấu trúc mã nguồn sạch. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Performance] | [src/engine/verification.py:81] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): load_rules (Complexity: 15 > 12). AI Note: Độ phức tạp 15 vượt ngưỡng 12 được thiết lập. Dù mức vi phạm không quá lớn nhưng vẫn cần được tái cấu trúc để đảm bảo hàm load_rules thực hiện nhiệm vụ đơn giản và dễ kiểm soát hơn. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Reliability] | [src/config.py:30] | Lý do: Sử dụng hàm ép kiểu trực tiếp `int()` trên biến môi trường `TEST_MODE_LIMIT_FILES` mà không có khối try/except hoặc giá trị mặc định an toàn khi parse. Nếu biến môi trường này bị cấu hình sai (ví dụ: '10 files'), ứng dụng sẽ crash ngay lập tức khi khởi động. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/api/audit_state.py:84] | Lý do: Lớp `AuditState` sử dụng các class attributes (`is_cancelled`, `is_running`) để quản lý trạng thái phiên kiểm toán. Trong môi trường FastAPI/Uvicorn (đa luồng/async), các biến này được chia sẻ chung giữa tất cả các request. Nếu một người dùng nhấn 'Cancel', toàn bộ tiến trình kiểm toán của các người dùng khác cũng sẽ bị hủy bỏ (Race Condition/Shared State Corruption). | Trình bày: AI_REASONING | Trọng số: -7.0
- [Reliability] | [src/api/audit_state.py:35] | Lý do: Trong phương thức `create_job`, lệnh `cls.legacy_logs.clear()` xóa sạch log cũ mỗi khi một job mới được tạo. Nếu có nhiều job chạy song song hoặc nối tiếp nhau nhanh chóng, log của các job trước đó đang được stream về frontend sẽ bị mất đột ngột, gây sai lệch dữ liệu hiển thị. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/api/api_server.py:22] | Lý do: Thực hiện 'monkey-patching' trực tiếp vào `starlette.formparsers.MultiPartParser.__init__`. Đây là một hành vi nguy hiểm vì nó can thiệp vào tầng core của framework. Khi Starlette nâng cấp phiên bản, chữ ký của hàm `__init__` hoặc cách xử lý nội bộ thay đổi sẽ dẫn đến lỗi hệ thống khó debug hoặc các lỗi bảo mật liên quan đến resource exhaustion (DoS) do giới hạn file quá lớn (100,000). | Trình bày: AI_REASONING | Trọng số: -5.0
- [Security] | [src/api/git_helper.py:51] | Lý do: Mã nguồn thực hiện tiêm trực tiếp `username` và `token` vào chuỗi URL để clone repository. Mặc dù có nỗ lực filter log ở dòng 79, nhưng URL đầy đủ chứa credential này thường sẽ bị lưu lại trong file cấu hình `.git/config` của thư mục đích sau khi clone thành công, dẫn đến rò rỉ thông tin nhạy cảm trên disk (Credential Leakage). | Trình bày: AI_REASONING | Trọng số: -6.0
- [Reliability] | [src/api/routers/audit.py:40] | Lý do: Việc ghi đè trực tiếp 'sys.stdout' sang 'StdoutFallbackStream' trong môi trường bất đồng bộ (FastAPI/asyncio) là một lỗi thiết kế nghiêm trọng. Vì 'sys.stdout' là biến toàn cục của tiến trình, nếu có nhiều yêu cầu audit chạy cùng lúc, log của các job sẽ bị trộn lẫn vào nhau. Hơn nữa, nếu hàm 'run_auditor_with_capture' gặp lỗi cực trọng và không thoát qua khối 'finally', 'sys.stdout' sẽ bị kẹt ở trạng thái chuyển hướng, làm hỏng toàn bộ hệ thống logging của server. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Reliability] | [src/api/routers/audit.py:105] | Lý do: Kiểm tra 'AuditState.is_running' trước khi gán giá trị mới không phải là một thao tác nguyên tử (atomic). Trong môi trường concurrent, hai request có thể cùng đi qua điều kiện kiểm tra (dòng 105) trước khi request đầu tiên kịp gán 'is_running = True' (dòng 58, thông qua run_auditor_with_capture), dẫn đến việc chạy song song nhiều phiên audit mà hệ thống vốn dĩ muốn chặn. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Security] | [src/api/routers/audit.py:157] | Lý do: Endpoint '/audit' (legacy) nhận tham số 'target' từ Query string và sử dụng 'os.path.abspath(target)' mà không có bước validate xem đường dẫn đó có nằm trong vùng được phép hay không. Điều này cho phép kẻ tấn công thực hiện kỹ thuật Path Traversal để yêu cầu hệ thống quét các thư mục nhạy cảm bên ngoài root dự án (ví dụ: /etc, /var/log). | Trình bày: AI_REASONING | Trọng số: -8.0
- [Reliability] | [src/engine/ai_service.py:173] | Lý do: Trong hàm 'deep_audit_batch', logic xử lý JSON lỗi (dòng 173-176) sử dụng regex để thay đổi chuỗi thô ('re.sub') trước khi parse. Việc tự động sửa 'backslash' trong chuỗi có thể dẫn đến làm sai lệch dữ liệu mã nguồn được báo cáo hoặc tạo ra JSON không hợp lệ nếu 'backslash' đó là một phần hợp lệ của chuỗi regex hoặc ký tự escape trong code đang scan. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -4.0
- [Maintainability] | [src/api/routers/audit.py:33] | Lý do: Tên hàm 'run_auditor_with_capture' gây hiểu lầm vì nó không chỉ thực hiện capture output mà còn đóng vai trò quản lý state (AuditState.is_running) và database rules injection. Ngoài ra, việc dùng 'safe_parts[0]' để xác định 'project_name' khi upload file hàng loạt (dòng 118) là không an toàn nếu danh sách file không đồng nhất cấu trúc thư mục. | Trình bày: MISLEADING_NAME | Trọng số: -3.0
- [Maintainability] | [src/engine/dependency_checker.py:25] | Lý do: Logic bug trong việc xác định tên module: Sử dụng `os.path.basename(f)` sẽ gây ra xung đột (name collision) nếu dự án có các file trùng tên ở các thư mục khác nhau (ví dụ: `auth/utils.py` và `db/utils.py`). Điều này khiến bản đồ phụ thuộc `mod_to_file` bị ghi đè dữ liệu, dẫn đến việc phát hiện phụ thuộc vòng (Circular Dependency) bị sai lệch hoàn toàn hoặc bỏ sót lỗi. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Security] | [src/engine/discovery.py:78] | Lý do: Rủi ro thực thi mã (Code Execution) và gây ô nhiễm môi trường kiểm toán: Hàm `run_discovery` tạo ra một script Python (`ai_precheck.py`) bên trong thư mục mục tiêu và thực thi nó thông qua `subprocess`. Nếu thư mục mục tiêu chứa mã nguồn không đáng tin cậy hoặc có file trùng tên từ trước, hành động này có thể bị lợi dụng để thực thi mã độc hoặc làm thay đổi trạng thái của mã nguồn đang được kiểm toán. | Trình bày: AI_REASONING | Trọng số: -8.0
- [Reliability] | [src/engine/auditor.py:190] | Lý do: Ép kiểu dữ liệu (float conversion) không an toàn trên input từ AI. Giá trị `rv.get('weight')` lấy từ `ai_service` (kết quả trả về từ LLM) có thể không phải là một chuỗi số hợp lệ. Việc gọi `float()` trực tiếp mà không có try/except sẽ gây crash chương trình khi nhận dữ liệu không mong muốn (SILENT_DATA_CORRUPTION - đầu vào ngoại vi không tin cậy). | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Security] | [src/engine/database.py:19] | Lý do: Lộ lọt thông tin xác thực (Hardcoded Credentials): Biến `DB_URL` chứa mật khẩu mặc định của PostgreSQL (`postgrespassword`). Ngay cả khi đây là giá trị mặc định cho Docker, việc để plaintext credentials trong mã nguồn là vi phạm tiêu chuẩn bảo mật. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Maintainability] | [src/engine/scoring.py:43] | Lý do: Tên hàm và tài liệu mô tả mâu thuẫn với giá trị trả về thực tế. Docstring ghi 'Calculates the final weighted score (0-10)', nhưng mã nguồn thực hiện nhân 10 ở dòng cuối `round(final * 10, 2)`, dẫn đến kết quả trả về là thang điểm 0-100. Điều này gây hiểu lầm cho các module tích hợp sử dụng điểm số này. | Trình bày: MISLEADING_NAME | Trọng số: -3.0
- [Reliability] | [src/engine/verification.py:98] | Lý do: Ép kiểu không an toàn (`float()`) trên dữ liệu `custom_weights` đến từ bên ngoài (AI Compiler hoặc User Config) mà không có khối bảo vệ try/except. Nếu giá trị trọng số truyền vào không phải là số hợp lệ, hệ thống sẽ crash hoàn toàn khi khởi động quá trình audit. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/engine/scanners.py:124] | Lý do: Lỗi logic nghiêm trọng trong việc thu thập luật AST: Biến `dangerous_functions_rule` bị ghi đè liên tục trong vòng lặp `for r in rules.get('rules', [])`. Nếu người dùng định nghĩa nhiều luật liên quan đến 'dangerous_functions', hệ thống chỉ ghi nhận và thực thi luật cuối cùng được tìm thấy, bỏ qua tất cả các luật trước đó. | Trình bày: AI_REASONING | Trọng số: -6.0
- [Reliability] | [src/engine/verification.py:159] | Lý do: Xử lý kết quả subprocess thiếu an toàn. `stdout_lines[-1]` sẽ gây lỗi `IndexError` nếu lệnh python thực thi verification script không trả về output nào hoặc chỉ trả về dòng trắng. Ngoài ra, việc luôn lấy dòng cuối cùng làm JSON là rủi ro nếu có log rác hoặc warning xuất hiện sau kết quả chính. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -4.0
- [Maintainability] | [src/engine/verification.py:129] | Lý do: Mâu thuẫn schema (Schema Mismatch): Trong `load_rules`, code kiểm tra `isinstance(custom.get('ast_rules'), list)`, nhưng trong `NaturalRulesCompiler`, example_json định nghĩa `ast_rules` là một Dictionary (`{"dangerous_functions": [...]}`). Điều này khiến toàn bộ luật AST do AI sinh ra bị bỏ qua hoàn toàn do sai lệch kiểu dữ liệu. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Performance] | [src/engine/scanners.py:204] | Lý do: Phát hiện N+1 Query lấp lửng trong logic `scan`. Vòng lặp `for subnode in ast.walk(node)` chứa lệnh `break` ngay sau khi tìm thấy call đầu tiên vi phạm. Điều này có nghĩa là nếu một vòng lặp chứa nhiều câu query khác nhau (ví dụ: fetches từ 2 bảng khác nhau), chỉ bảng đầu tiên được báo cáo, có thể che giấu các vấn đề hiệu năng nghiêm trọng khác trong cùng một block code. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Performance] | [tests/test_natural_rules_compiler.py:19] | Lý do: Trong vòng lặp `async for chunk in compiler.compile_rules_stream(prompt):`, chuỗi `full_output` được tích lũy bằng toán tử `+=`. Vì string trong Python là immutable, mỗi lần cộng chuỗi sẽ tạo ra một bản sao mới, dẫn đến độ phức tạp O(N^2). Với các output từ LLM có thể dài, việc này gây lãng phí bộ nhớ và CPU. Nên sử dụng một list để append các chunk và dùng `''.join()` sau khi kết thúc loop. | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Reliability] | [src/engine/auditor.py:15] | Lý do: Lớp CodeAuditor được khởi tạo và chạy tại main.py nhưng chưa rõ cách xử lý các path không tồn tại hoặc lỗi quyền truy cập file. [Cross-Checked: Bằng chứng mã nguồn xác nhận CodeAuditor chỉ thực hiện os.path.abspath cho target_dir mà không kiểm tra sự tồn tại (os.path.exists) hoặc quyền truy cập trước khi truyền vào DiscoveryStep. Nếu đường dẫn không hợp lệ, chương trình sẽ gặp lỗi RuntimeError/FileNotFoundError khi thực thi subprocess ở các bước sau mà không có cơ chế bắt lỗi (try-except) bọc ngoài ở tầng cao nhất của hàm run().] | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [tests/test_natural_rules_compiler.py:49] | Lý do: Kết quả trả về từ `AuditDatabase.get_project_rules('test_target')` được truy cập trực tiếp bằng `res['natural_text']` mà không kiểm tra None. Các hàm truy vấn database thường trả về None nếu không tìm thấy dữ liệu (ví dụ record chưa kịp commit hoặc query fail), gây ra lỗi AttributeError/TypeError khi chạy. [Cross-Checked: Bằng chứng mã nguồn của get_project_rules cho thấy hàm này trả về None một cách rõ ràng nếu không tìm thấy bản ghi (row) trong database. Do đó, việc mã nguồn kiểm thử truy cập trực tiếp vào kết quả trả về như res['natural_text'] mà không có kiểm tra `if res:` là một rủi ro thực sự có thể gây sập chương trình kiểm thử.] | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
