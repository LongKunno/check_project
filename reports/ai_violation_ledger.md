# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Maintainability] | [src/api/routers/audit.py:830] | Lý do: Function too long (God Object anti-pattern): audit_batch (> 150 lines). AI Note: Đây nhiều khả năng là lỗi thật: hàm `audit_batch` được cảnh báo là quá dài (>150 dòng), và mô tả cho thấy đây là một hàm xử lý nhiều nhánh/luồng nghiệp vụ, phù hợp với vấn đề maintainability/God Object. | Trình bày: GOD_OBJECT | Điểm phạt: -2.0
- [Maintainability] | [src/api/routers/repositories.py:210] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): update_engine_settings (Complexity: 26 > 25). AI Note: Đây nhiều khả năng là lỗi thật: `update_engine_settings` có cyclomatic complexity 26 vượt ngưỡng 25. Ngưỡng này cho thấy hàm có quá nhiều nhánh rẽ/điều kiện, làm giảm khả năng bảo trì. | Trình bày: HIGH_COMPLEXITY | Điểm phạt: -2.0
- [Security] | [src/engine/ai_telemetry.py:1322] | Lý do: Potential SQL Injection (Insecure string interpolation in DB query). AI Note: Đoạn trích không cho thấy dữ liệu người dùng được nối trực tiếp vào câu lệnh SQL; chỉ thấy mở đầu một f-string. Nếu phần còn lại không nội suy biến vào SQL thì không phải lỗ hổng injection. Với snippet hiện tại, đây là cảnh báo chưa đủ căn cứ. | Trình bày: SQL_INJECTION | Điểm phạt: -8.0
- [Security] | [src/engine/ai_telemetry.py:1431] | Lý do: Potential SQL Injection (Insecure string interpolation in DB query). AI Note: Đây là dấu hiệu SQL Injection thực sự vì câu lệnh SQL được tạo bằng f-string và chèn trực tiếp biến `{clause}` vào câu lệnh: `f"SELECT COUNT(*) AS count FROM ai_request_logs {clause}"`. Nếu `clause` có thể bị ảnh hưởng bởi input bên ngoài, truy vấn sẽ không an toàn. Nên dùng tham số hóa hoặc whitelist chặt chẽ cho phần động này. | Trình bày: SQL_INJECTION | Điểm phạt: -8.0
- [Security] | [src/engine/ai_telemetry.py:1436] | Lý do: Potential SQL Injection (Insecure string interpolation in DB query). AI Note: Đoạn trích chỉ cho thấy `cursor.execute(f"""...` nhưng không có dấu hiệu nào cho thấy biến do người dùng kiểm soát được nối trực tiếp vào SQL. Theo yêu cầu, chỉ coi là lỗi khi có user input được chèn thẳng bằng f-string/.format(). Với thông tin hiện có, chưa đủ căn cứ khẳng định SQL Injection. | Trình bày: SQL_INJECTION | Điểm phạt: -8.0
- [Security] | [src/engine/ai_telemetry.py:1505] | Lý do: Potential SQL Injection (Insecure string interpolation in DB query). AI Note: Chỉ thấy `cursor.execute(f"""...` nhưng chưa thấy phần nội suy biến nào từ input người dùng. F-string tự nó chưa đủ để kết luận có SQL Injection nếu không có dữ liệu không tin cậy được chèn vào câu lệnh. | Trình bày: SQL_INJECTION | Điểm phạt: -8.0
- [Security] | [src/engine/ai_telemetry.py:1821] | Lý do: Potential SQL Injection (Insecure string interpolation in DB query). AI Note: Đoạn mã được cung cấp không cho thấy biến đầu vào nào được nối trực tiếp vào SQL. Không có bằng chứng về việc chèn user input vào chuỗi truy vấn, nên không thể xác nhận đây là lỗi thật. | Trình bày: SQL_INJECTION | Điểm phạt: -8.0
- [Maintainability] | [src/engine/ai_telemetry.py:574] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): get_filter_metadata (Complexity: 30 > 25). AI Note: Không có đủ ngữ cảnh để bác bỏ cảnh báo, và báo cáo độ phức tạp cyclomatic 30 > 25 là một vi phạm hợp lệ nếu công cụ phân tích đã đo đúng trên hàm `get_filter_metadata`. | Trình bày: HIGH_COMPLEXITY | Điểm phạt: -2.0
- [Maintainability] | [src/engine/ai_telemetry.py:906] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): _filter_memory_rows (Complexity: 26 > 25). AI Note: Tương tự #3, cảnh báo về độ phức tạp cyclomatic 26 > 25 là hợp lệ theo số đo được cung cấp, nên xem là lỗi thật nếu kết quả phân tích tĩnh là chính xác. | Trình bày: HIGH_COMPLEXITY | Điểm phạt: -2.0
- [Maintainability] | [src/engine/ai_telemetry.py:1525] | Lý do: Function too long (God Object anti-pattern): get_overview (> 150 lines). AI Note: Thông báo cho thấy hàm get_overview vượt quá 150 dòng và độ phức tạp cyclomatic 33 > 25. Với ngưỡng như vậy, đây nhiều khả năng là vi phạm thật của maintainability (hàm quá dài/quá phức tạp), không phải báo lỗi sai. | Trình bày: GOD_OBJECT | Điểm phạt: -2.0
- [Maintainability] | [src/engine/ai_telemetry.py:1525] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): get_overview (Complexity: 33 > 25). AI Note: Cyclomatic complexity được báo là 33, cao hơn ngưỡng 25. Đây thường là dấu hiệu thật của hàm có nhiều nhánh/điều kiện và cần refactor, nên tôi xem đây là True Positive. | Trình bày: HIGH_COMPLEXITY | Điểm phạt: -2.0
- [Reliability] | [src/engine/ai_telemetry.py:837] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Đây là một `except Exception: pass` đúng nghĩa nuốt exception im lặng: không log, không raise lại. Với đoạn snippet này không có dấu hiệu rõ ràng là chủ đích bỏ qua lỗi quan trọng, nên nên xem là lỗi thật. | Trình bày: SWALLOWED_EXCEPTION | Điểm phạt: -5.0
- [Reliability] | [src/engine/ai_telemetry.py:1233] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Tương tự vi phạm #0, đây là `except Exception: pass` không xử lý gì thêm, nên exception bị nuốt im lặng. Không thấy dấu hiệu đây là bỏ qua lỗi có chủ đích. | Trình bày: SWALLOWED_EXCEPTION | Điểm phạt: -5.0
- [Maintainability] | [src/engine/auditor.py:1164] | Lý do: Function too long (God Object anti-pattern): _step_ai_reasoning (> 150 lines). AI Note: Thông tin cho thấy hàm `_step_ai_reasoning` vượt quá ngưỡng 150 dòng theo rule kiểm tra. Đây là cảnh báo maintainability hợp lệ, không phải false positive dựa trên mô tả hiện có. | Trình bày: GOD_OBJECT | Điểm phạt: -2.0
- [Maintainability] | [src/engine/auditor.py:1545] | Lý do: Function too long (God Object anti-pattern): _step_ai_reasoning_batch_api (> 150 lines). AI Note: Thông tin cho thấy hàm `_step_ai_reasoning_batch_api` vượt quá ngưỡng 150 dòng theo rule kiểm tra. Đây là cảnh báo maintainability hợp lệ, không phải false positive dựa trên mô tả hiện có. | Trình bày: GOD_OBJECT | Điểm phạt: -2.0
- [Reliability] | [src/engine/auditor.py:1913] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Đây là một except Exception: pass, nhưng theo đoạn trích không thấy bối cảnh cho biết đó là lỗi quan trọng hay chỉ là bỏ qua các trường hợp không hợp lệ/không cần thiết. Vì chưa có bằng chứng rằng lỗi bị nuốt sai mục đích, nên tạm coi là false positive. | Trình bày: SWALLOWED_EXCEPTION | Điểm phạt: -5.0
- [Security] | [src/api/api_server.py:19] | Lý do: Tệp này monkey-patch toàn cục `starlette.formparsers.MultiPartParser.__init__` để nâng giới hạn upload lên `max_files=10000` và `max_fields=10000` cho mọi request. Việc tăng trần parser ở mức global như vậy làm mở rộng đáng kể bề mặt tấn công DoS/Resource Exhaustion: kẻ tấn công có thể gửi multipart với số lượng part rất lớn để tiêu tốn RAM/CPU trước khi ứng dụng có cơ hội áp dụng kiểm soát ở tầng business logic. Đây là thay đổi hành vi của framework ở phạm vi toàn ứng dụng, khó kiểm soát và khó audit. | Trình bày: AI_REASONING | Điểm phạt: -2.0
- [Maintainability] | [src/api/routers/audit.py:74] | Lý do: Hàm `delete_repository` ở router khác không có trong file này; trong file hiện tại, vi phạm rõ nhất là import deferred trong thân hàm. Cụ thể, `from src.engine.ai_service import ai_service` được import bên trong `_cancel_remote_batches_for_job()` và `get_fix_suggestion()`. Đây là E402/PEP8_MODULE_LEVEL_IMPORTS, làm phân mảnh namespace và gây khó theo dõi phụ thuộc. Nếu mục đích là tránh circular import, nên ghi chú rõ ràng hoặc tách kiến trúc thay vì import rải rác trong runtime path. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Maintainability] | [src/api/routers/repositories.py:116] | Lý do: Hàm `delete_repository` chỉ gọi `AuditDatabase.delete_repository(repo_id)` với chú thích soft-delete, nên tên hàm thể hiện hành vi xóa vĩnh viễn là gây hiểu lầm. Đây là vấn đề maintainability/API semantics, dễ làm người dùng hoặc đồng đội hiểu sai về vòng đời dữ liệu. | Trình bày: MISLEADING_NAME | Điểm phạt: -1.0
- [Maintainability] | [src/api/routers/repositories.py:58] | Lý do: Trong `update_engine_settings()`, các import từ `src.config` được thực hiện bên trong hàm `_build_engine_settings_payload()`. Đây là deferred import nằm trong thân hàm, vi phạm PEP8 module-level imports nếu không có chứng cứ rõ ràng về plugin bootstrap/circular dependency. Nó làm logic cấu hình khó truy vết và tăng độ phức tạp runtime. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Maintainability] | [src/api/routers/rules.py:28] | Lý do: File có nhiều import được đặt trong thân hàm như `_run_scanners_sync()`, `compile_rules()`, và `auto_fix_rule()`. Đây là deferred import/lazy import nhưng không có chú thích hay bằng chứng rõ ràng về circular import hoặc optional dependency. Việc này làm phân mảnh quản lý dependency và khiến luồng khởi tạo khó theo dõi. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Reliability] | [src/engine/ai_cache.py:14] | Lý do: Hàm `_utc_now()` tạo `datetime.now(UTC)` rồi ngay sau đó gọi `.replace(tzinfo=None)`, làm mất thông tin múi giờ và biến giá trị thành datetime naive. Điều này dễ gây lệch múi giờ khi lưu/so sánh thời gian, đặc biệt vì các chỗ khác trong module lại dùng ISO string và so sánh theo timestamp. | Trình bày: NAIVE_DATETIME | Điểm phạt: -2.0
- [Reliability] | [src/engine/ai_telemetry.py:20] | Lý do: Hàm `_utc_now()` tạo datetime bằng `datetime.now(UTC).replace(tzinfo=None)`, làm mất thông tin múi giờ và trả về thời gian naive. Dữ liệu telemetry/budget có thể bị lệch khi so sánh theo server timezone hoặc khi serialize/deserialize qua DB. | Trình bày: NAIVE_DATETIME | Điểm phạt: -2.0
- [Reliability] | [src/engine/auditor.py:701] | Lý do: Trong `list_requests()`, `page_size` và `page` được ép kiểu trực tiếp bằng `int()` rồi dùng để tính phân trang mà không có validate/try-except. Nếu dữ liệu đầu vào là chuỗi không hợp lệ (ví dụ từ API/query param), hàm sẽ ném exception hoặc xử lý sai, gây lỗi runtime hoặc hành vi không nhất quán. | Trình bày: SILENT_DATA_CORRUPTION | Điểm phạt: -5.0
- [Reliability] | [src/engine/auditor.py:315] | Lý do: `_step_discovery()` gọi `get_test_mode_limit()` bằng import cục bộ rồi dùng trực tiếp kết quả để cắt danh sách file. Đây là deferred import không có chú thích về circular dependency/optional dependency; theo chuẩn PEP8, import nên đặt ở đầu file trừ khi có lý do rõ ràng. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Reliability] | [src/engine/auditor.py:514] | Lý do: Trong `_step_ai_processing()`, `get_ai_max_concurrency()` được import bên trong hàm thay vì ở module level. Đây là lazy import trong logic xử lý chính, làm phân mảnh namespace và giảm khả năng đọc/kiểm soát phụ thuộc. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Maintainability] | [src/engine/auditor.py:409] | Lý do: `_build_cross_check_context()` import `AstContextExtractor` ngay trong thân hàm. Nếu không phải để tránh vòng lặp import hoặc dependency tùy chọn, đây là vi phạm PEP8 E402 (import không đặt ở đầu file) và làm code khó bảo trì hơn. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Maintainability] | [src/engine/auditor.py:1268] | Lý do: `_write_report_content()` thực hiện import `datetime` bên trong hàm dù đây là phụ thuộc chuẩn và không có lý do rõ ràng để trì hoãn. Import cục bộ kiểu này làm rối luồng phụ thuộc và không phù hợp với quy ước đặt import ở đầu file. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Maintainability] | [src/engine/auditor.py:1155] | Lý do: `_log_ai_violations()` import `WEIGHTS` cục bộ dù module này đã import `WEIGHTS` ở đầu file. Việc import lặp lại trong thân hàm là dư thừa, gây nhiễu và làm giảm tính nhất quán của module-level dependencies. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Maintainability] | [src/engine/database.py:790] | Lý do: Tên hàm `delete_repository()` gây hiểu lầm nghiêm trọng so với hành vi thực tế: hàm không xóa bản ghi mà chỉ soft-delete bằng cách set `is_active = FALSE`. Điều này có thể khiến người gọi hiểu sai về mức độ xóa dữ liệu và trạng thái tồn tại của repository. | Trình bày: MISLEADING_NAME | Điểm phạt: -1.0
- [Reliability] | [src/engine/database.py:531] | Lý do: `serialized_full_json = json.dumps(full_json) if full_json else None` làm mất toàn bộ payload khi `full_json` là dict rỗng hoặc giá trị falsey khác. Đây là dạng rò rỉ/mất dữ liệu âm thầm vì bản ghi audit sẽ bị lưu thiếu nội dung gốc và metadata gắn `audit_id` có thể không còn phản ánh đúng trạng thái đã ghi. | Trình bày: SILENT_DATA_CORRUPTION | Điểm phạt: -5.0
- [Reliability] | [src/engine/database.py:454] | Lý do: Các hàm đọc lịch sử như `get_history()`, `get_latest_audits_for_targets()` và `get_audit_by_id()` parse lại JSON từ cột TEXT nhưng chỉ xử lý một phần lỗi parse. Khi dữ liệu trong DB đã bị hỏng định dạng, hàm có thể trả về cấu trúc thiếu trường hoặc sai kiểu mà không báo lỗi rõ ràng, gây khó debug và có thể làm hỏng downstream processing. | Trình bày: AI_REASONING | Điểm phạt: -2.0
- [Maintainability] | [src/engine/discovery.py:15] | Lý do: Hàm `generate_precheck_script()` tạo ra một Python script lớn bằng f-string đa dòng với nhiều khối code lồng nhau. Đây là cấu trúc khó bảo trì, dễ lỗi escape/indent và che khuất lỗi runtime vì code thật chỉ xuất hiện dưới dạng chuỗi được sinh động. Mô hình này làm giảm khả năng kiểm thử tĩnh và tăng rủi ro sai lệch giữa generator và script thực thi. | Trình bày: AI_REASONING | Điểm phạt: -1.5
- [Maintainability] | [src/engine/scanners.py:54] | Lý do: `_check_functions()` và các helper liên quan đang gộp nhiều luật kiểm tra khác nhau trong một lớp scanner đơn lẻ, khiến trách nhiệm quá lớn và làm tăng rủi ro thay đổi chéo giữa các rule AST. Đây là dấu hiệu của God Object/violation of Separation of Concerns và sẽ khó mở rộng khi thêm rule mới. | Trình bày: AI_REASONING | Điểm phạt: -1.0
- [Maintainability] | [src/engine/verification.py:76] | Lý do: `import tempfile` được đặt bên trong `VerificationStep.__init__` thay vì ở đầu file. Đây là deferred import không có lý do rõ ràng về optional dependency hay circular import, làm phân mảnh namespace và khó bảo trì hơn theo PEP8 E402. | Trình bày: PEP8_MODULE_LEVEL_IMPORTS | Điểm phạt: -1.5
- [Maintainability] | [src/engine/database.py:660] | Lý do: `toggle_core_rule()` có logic đọc-modify-write trên cùng một row với `SELECT ... FOR UPDATE`, nhưng khi `project_rules` chưa tồn tại thì hàm tự chèn row mặc định rồi đọc lại. Nhánh này phụ thuộc mạnh vào trạng thái DB và có thể tạo hành vi khó dự đoán nếu có concurrent writers hoặc dữ liệu bị seed không nhất quán; cần xác minh kỹ luồng gọi từ nơi khác trước khi kết luận an toàn. | Trình bày: AI_REASONING | Điểm phạt: -2.0
- [Reliability] | [src/engine/database.py:504] | Lý do: `save_audit()` dùng `previous_audit = _load_previous_audit_row(conn, target)` rồi tính regression snapshot ngay trong cùng luồng ghi. Nếu có nhiều tiến trình/worker ghi audit song song cho cùng một target, baseline có thể bị chọn theo trạng thái trước commit của giao dịch khác, dẫn đến regression status sai hoặc không nhất quán. | Trình bày: AI_REASONING | Điểm phạt: -2.0
