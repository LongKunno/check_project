# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Maintainability] | [src/api/api_server.py:8] | Lý do: Import 'io' might be unused (AST). AI Note: Import 'io' không được sử dụng ở bất kỳ đâu trong file /src/api/api_server.py. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Performance] | [src/api/git_helper.py:12] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): clone_repository (Complexity: 13 > 12). AI Note: Hàm 'clone_repository' có cấu trúc rẽ nhánh phức tạp với nhiều khối try-except lồng nhau và các điều kiện kiểm tra (if), dẫn đến độ phức tạp Cyclomatic thực tế vượt quá ngưỡng 12. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/api/git_helper.py:3] | Lý do: Import 'urllib.parse' might be unused (AST). AI Note: Import 'urllib.parse' không được sử dụng trực tiếp trong file /src/api/git_helper.py. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Performance] | [src/api/routers/audit.py:92] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): upload_and_audit (Complexity: 14 > 12). AI Note: Hàm 'upload_and_audit' chứa nhiều logic rẽ nhánh (if/else) để kiểm tra file, xử lý lỗi, và lồng ghép vòng lặp xử lý danh sách file. Việc quản lý nhiều luồng thực thi (exception handling, file type checking) khiến độ phức tạp vượt mức khuyến nghị. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Performance] | [src/engine/dependency_checker.py:11] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): detect_circular_dependencies (Complexity: 21 > 12). AI Note: Hàm 'detect_circular_dependencies' có cấu trúc lồng nhau rất phức tạp: xử lý tệp, phân tích AST, duyệt đồ thị để tìm chu trình (DFS lồng trong vòng lặp), cùng nhiều điều kiện rẽ nhánh. Độ phức tạp tính toán vượt quá ngưỡng khuyến nghị (12). | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Reliability] | [src/engine/dependency_checker.py:42] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Khối 'except Exception: pass' nuốt mọi lỗi phát sinh trong quá trình phân tích AST hoặc xử lý phụ thuộc mà không có ghi nhật ký (log) hay xử lý cụ thể, điều này gây khó khăn cho việc gỡ lỗi khi có tệp bị hỏng hoặc lỗi định dạng. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Performance] | [src/engine/ai_service.py:277] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Đoạn mã thực hiện nối chuỗi bằng toán tử `+=` bên trong một vòng lặp (dựa vào thụt đầu dòng và các biến i, v thường dùng trong enumerate). Trong Python, chuỗi là bất biến nên việc cộng chuỗi liên tục sẽ tạo ra nhiều đối tượng tạm thời, gây tốn bộ nhớ và giảm hiệu suất đáng kể so với việc dùng list.append() và ''.join(). | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Performance] | [src/engine/ai_service.py:278] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Tương tự như vi phạm #0, việc nối chuỗi `items_prompt +=` diễn ra bên trong vòng lặp xử lý các kết quả (items). Đây là lỗi Performance True Positive theo quy tắc đã nêu. | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Performance] | [src/engine/ai_service.py:279] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Đoạn mã này cũng thuộc cùng một khối logic lặp, thực hiện nối các chuỗi f-string vào biến `items_prompt`. Việc lặp lại hành động này nhiều lần trong vòng lặp là không tối ưu. | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Performance] | [src/engine/ai_service.py:280] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Tiếp nối phần nội dung của các vi phạm trước đó, đây vẫn là hành vi nối chuỗi trong vòng lặp. Cần thay thế bằng cơ chế gom danh sách rồi mới join. | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Reliability] | [src/engine/database.py:153] | Lý do: Bare except: block detected (Catches SystemExit/KeyboardInterrupt). AI Note: Sử dụng 'except:' mà không chỉ định loại ngoại lệ sẽ bắt tất cả các lỗi, bao gồm cả SystemExit và KeyboardInterrupt. Điều này làm cản trở việc dừng chương trình và gây khó khăn khi debug. Nên sử dụng 'except json.JSONDecodeError:' hoặc 'except Exception:'. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Maintainability] | [src/engine/database.py:38] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Trong code production, việc sử dụng hàm print() để ghi log là không chuyên nghiệp vì không hỗ trợ các cấp độ log (INFO, ERROR), không cấu hình được định dạng log tập trung và không tích hợp tốt với các hệ thống giám sát. Nên sử dụng thư viện logging. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Reliability] | [src/engine/database.py:128] | Lý do: Bare except: block detected (Catches SystemExit/KeyboardInterrupt). AI Note: Lỗi tương tự vi phạm số 0. Một khối bare except được sử dụng khi giải mã JSON, điều này có thể che giấu các lỗi hệ thống không mong muốn thay vì chỉ bắt lỗi phân tích cú pháp dữ liệu. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Reliability] | [src/engine/database.py:159] | Lý do: Bare except: block detected (Catches SystemExit/KeyboardInterrupt). AI Note: Lỗi tương tự vi phạm số 0. Việc bắt mọi ngoại lệ khi xử lý JSON là một thực hành xấu (bad practice) về độ tin cậy của mã nguồn. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Reliability] | [src/engine/database.py:203] | Lý do: Bare except: block detected (Catches SystemExit/KeyboardInterrupt). AI Note: Lỗi tương tự vi phạm số 0 kết hợp với từ khóa 'pass' tạo thành một khối mã 'nuốt' mọi lỗi lầm mà không để lại bất kỳ dấu vết nào, cực kỳ nguy hiểm cho việc duy trì hệ thống. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Maintainability] | [src/engine/auditor.py:97] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Việc sử dụng hàm print() trong mã nguồn production (đặc biệt là trong các module xử lý logic tại src/engine/) là vi phạm tiêu chuẩn về Maintainability. Cần sử dụng thư viện logging để có thể quản lý log level, redirect output và lưu trữ log tốt hơn. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:156] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Thông báo tiến trình nên được ghi nhận qua logger.info() thay vì print() để đảm bảo tính nhất quán và khả năng cấu hình của hệ thống ghi log. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:206] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Dòng thông tin về batching này mang tính chất debug/informational. Sử dụng logger giúp người vận hành có thể tắt/mở thông tin này bằng cách thay đổi log level mà không cần sửa code. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:281] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Việc in trực tiếp trạng thái tiến trình ra console bằng print() trong logic xử lý chính của engine là True Positive. Nên thay thế bằng logger. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:406] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Tương tự các trường hợp trên, đây là mã nguồn production trong engine, không nên sử dụng print() để tương tác với người dùng hoặc debug. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/natural_rules_compiler.py:5] | Lý do: Import 'Optional' might be unused (AST). AI Note: Dựa trên các đoạn mã được cung cấp, 'Optional' có vẻ như không được sử dụng trong các định nghĩa kiểu dữ liệu phía sau. Nếu không có biến nào được khai báo với kiểu Optional[...], đây là một import thừa. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:247] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng print() trong mã nguồn của engine chuyên dụng (src/engine/auditor.py) là vi phạm quy tắc Maintainability. Nên sử dụng thư viện logging (như logger.info) để có thể quản lý đầu ra, định dạng và cấp độ tin nhắn tốt hơn. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:80] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Thông báo về chế độ test mode nên được ghi lại thông qua logger.warning thay vì print(), điều này giúp quá trình gỡ lỗi và quản lý log trong môi trường production chuyên nghiệp hơn. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:121] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Thông báo lỗi/cảnh báo về việc hủy tác vụ ngay trước khi raise Exception nên được ghi vào log để lưu trữ dấu vết hệ thống, thay vì in ra stdout bằng print(). | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:149] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Các thông tin chi tiết về kết quả xử lý của AI nên sử dụng log level INFO hoặc DEBUG. Việc sử dụng print() làm cho việc tích hợp code này vào các hệ thống khác (như CI/CD) khó quản lý đầu ra hơn. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:221] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Tương tự như vi phạm #2, việc thông báo trạng thái hủy bỏ bằng print() là không phù hợp trong mã nguồn logic của engine. Cần chuyển sang sử dụng logger. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:40] | Lý do: Function has too many parameters (Consider DTO/Dict): log_violation (9 > 7). AI Note: Hàm `log_violation` có tổng cộng 9 tham số (bao gồm cả 'self'). Theo tiêu chuẩn Maintainability (khả năng bảo trì), việc một hàm có quá nhiều tham số (thường ngưỡng là 7) sẽ làm code khó đọc và khó test hơn. Nên sử dụng một Data Transfer Object (DTO) hoặc Dictionary để nhóm các tham số lại. | Trình bày: TOO_MANY_PARAMS | Trọng số: -3.0
- [Reliability] | [src/api/routers/rules.py:34] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Việc sử dụng 'except Exception: pass' bao quanh câu lệnh 'ast.parse()' là một lỗi Reliability thực sự. Khi nội dung file không hợp lệ (SyntaxError) hoặc có lỗi hệ thống, biến 'tree' sẽ không được khởi tạo (hoặc giữ giá trị cũ), dẫn tới lỗi NameError ở các dòng code phía sau. Việc im lặng bỏ qua mà không có log khiến việc debug cực kỳ khó khăn. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Reliability] | [src/api/routers/rules.py:40] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Đoạn mã này nuốt mọi lỗi xảy ra trong quá trình scanner thực hiện quét file. Nếu scanner gặp lỗi logic hoặc lỗi truy cập dữ liệu, hệ thống sẽ bỏ qua toàn bộ vi phạm của file đó mà không có bất kỳ thông báo hay dấu vết nào trong logs, vi phạm nguyên tắc Reliability. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Performance] | [src/engine/ai_service.py:117] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Đây là lỗi Performance chính xác. Phép toán `+=` trên chuỗi trong vòng lặp 'for' gây ra độ phức tạp O(n^2) do chuỗi trong Python là immutable (mỗi lần nối lại tạo ra một bản copy mới). Với số lượng file lớn hoặc nội dung file dài, việc này sẽ làm chậm đáng kể quá trình xử lý. | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Performance] | [src/engine/ai_service.py:205] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Tương tự như vi phạm #2, biến 'items_prompt' đang được cộng dồn chuỗi liên tục bên trong một vòng lặp (dựa trên biến i). Cách tiếp cận tối ưu là sử dụng một list để chứa các phần tử và dùng ''.join() ở cuối. | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Performance] | [src/engine/ai_service.py:276] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Đoạn mã thực hiện nhiều phép cộng chuỗi `+=` liên tiếp bên trong vòng lặp. Điều này nhân đôi vấn đề về hiệu năng bộ nhớ và CPU cho mỗi vòng lặp so với việc sử dụng phương pháp join(). | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Maintainability] | [src/engine/auditor.py:60] | Lý do: Function too long (God Object anti-pattern): run (> 80 lines). AI Note: Hàm 'run' trong các công cụ audit thường đóng vai trò điều phối (orchestrator). Việc vượt quá 80 dòng là lỗi thật (True Positive) vì nó vi phạm nguyên tắc Single Responsibility, khiến mã nguồn khó bảo trì và kiểm thử đơn vị. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Performance] | [src/engine/auditor.py:60] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): run (Complexity: 56 > 12). AI Note: Độ phức tạp Cyclomatic lên tới 56 là cực kỳ cao (ngưỡng thông thường là 10-15). Điều này cho thấy hàm 'run' có quá nhiều nhánh rẽ nhánh hoặc vòng lặp lồng nhau, gây khó khăn cho việc bao phủ kiểm thử (test coverage). | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/auditor.py:418] | Lý do: Function too long (God Object anti-pattern): generate_report (> 80 lines). AI Note: Hàm 'generate_report' thường chứa nhiều logic định dạng văn bản và xử lý chuỗi. Việc dài hơn 80 dòng cho thấy logic tạo báo cáo chưa được mô-đun hóa thành các hàm nhỏ hơn cho từng phần của báo cáo. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Performance] | [src/engine/auditor.py:418] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): generate_report (Complexity: 22 > 12). AI Note: Độ phức tạp 22 vượt đáng kể ngưỡng 12. Việc xử lý kết quả phân cấp (feature_results, project_pillars) bằng nhiều vòng lặp lồng nhau trong một hàm duy nhất là nguyên nhân chính. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/auditor.py:66] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng hàm 'print' trong mã nguồn production (đặc biệt là trong folder 'engine') là một lỗi thật. Nên sử dụng thư viện logging để có thể quản lý log level, redirect log và lưu trữ log chuyên nghiệp hơn. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Reliability] | [src/engine/database.py:266] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Đây là một True Positive. Việc sử dụng 'except: pass' không chỉ nuốt các lỗi liên quan đến xử lý JSON (như JSONDecodeError) mà còn vô tình nuốt cả các ngoại lệ hệ thống như KeyboardInterrupt hoặc SystemExit. Trong code engine, ít nhất nên log lại lỗi hoặc bắt đúng ngoại lệ cụ thể. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Reliability] | [src/engine/database.py:271] | Lý do: Bare except: block detected (Catches SystemExit/KeyboardInterrupt). AI Note: Đây là một True Positive. Bare except (ngoại lệ không định danh) là một lỗi nghiêm trọng trong Python vì nó ngăn cản việc dừng chương trình (Ctrl+C). Nó vi phạm quy tắc PEP 8 và làm giảm độ tin cậy của hệ thống. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Reliability] | [src/engine/database.py:271] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Đây là một True Positive. Tương tự như vi phạm #0, việc bỏ qua hoàn toàn lỗi mà không có bất kỳ hình thức xử lý hay ghi log nào khiến việc debug trở nên cực kỳ khó khăn khi dữ liệu trong database bị sai định dạng. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Maintainability] | [src/engine/database.py:34] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Đây là một True Positive. Trong code dành cho môi trường production (thư mục src/engine), việc sử dụng 'print' thay vì 'logger' làm hạn chế khả năng quản lý log, lọc log theo level và lưu trữ log tập trung. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:13] | Lý do: Import 'AuditDatabase' might be unused (AST). AI Note: Dựa trên phân tích AST, nếu lớp 'AuditDatabase' được import nhưng không được tham chiếu ở bất kỳ đâu trong thân file, đây là một import thừa. Tuy nhiên, nếu file này được dùng như một interface để export các class, nó có thể là ý đồ của người viết. Nhưng thông thường trong code logic, đây là một lỗi Maintainability thực tế (Unused Import). | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Reliability] | [src/engine/authorship.py:85] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Sử dụng 'except Exception: pass' mà không có ghi chú giải thích hoặc log lại lỗi là một hành vi 'nuốt' ngoại lệ (swallowing exceptions). Điều này gây khó khăn cho việc debug và làm giảm độ tin cậy của hệ thống vì các lỗi không mong muốn sẽ bị bỏ qua âm thầm. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Security] | [src/engine/database.py:114] | Lý do: Potential SQL Injection (Insecure string interpolation in DB query). AI Note: Câu lệnh SQL sử dụng f-string (`f'...'`) để chèn biến `query_cols` trực tiếp vào chuỗi truy vấn. Đây là hành vi nối chuỗi (string interpolation) không an toàn, có thể dẫn đến tấn công SQL Injection nếu `query_cols` bị kẻ tấn công kiểm soát hoặc chứa nội dung không được kiểm chứng. | Trình bày: SQL_INJECTION | Trọng số: -8.0
- [Performance] | [src/engine/database.py:139] | Lý do: SELECT * in BigQuery/SQL (anti-pattern). AI Note: Đoạn mã thực thi một câu lệnh `SELECT *`, đây là một anti-pattern về hiệu suất trong SQL (đặc biệt là với các hệ thống OLAP như BigQuery hoặc các bảng có nhiều cột/dữ liệu lớn) vì nó lấy ra toàn bộ các cột thay vì chỉ những cột cần thiết. Đây là câu lệnh thực thi (`cursor.execute`), không phải trong comment. | Trình bày: SELECT_STAR | Trọng số: -2.0
- [Maintainability] | [src/engine/symbol_indexer.py:15] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Việc sử dụng lệnh 'print' trực tiếp trong mã nguồn production (đặc biệt là trong các module xử lý engine) là một vi phạm về Maintainability. Cần sử dụng thư viện logging để có thể quản lý các mức độ thông báo (DEBUG, INFO, ERROR) và dễ dàng chuyển hướng output khi cần thiết. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Reliability] | [src/engine/symbol_indexer.py:56] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Đoạn mã sử dụng 'except Exception: pass' để nuốt toàn bộ các lỗi mà không có logging hay xử lý lại. Điều này làm giảm khả năng Reliability và gây khó khăn cho việc gỡ lỗi vì các lỗi tiềm ẩn (như lỗi truy cập file, lỗi mã hóa) bị bỏ qua âm thầm. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Reliability] | [src/engine/database.py:203] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Việc sử dụng bare except kết hợp với 'pass' mà không có logging hay xử lý cụ thể là một lỗi Reliability. Nó nuốt tất cả các lỗi kể cả lỗi hệ thống (như KeyboardInterrupt) và khiến việc debug trở nên bất khả thi khi dữ liệu JSON bị hỏng. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Maintainability] | [src/engine/database.py:88] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Trong mã nguồn production, đặc biệt là ở lớp database, việc dùng 'print' là không tốt vì nó không hỗ trợ log levels, không lưu được vào file log chuyên dụng và khó quản lý trong môi trường container/server. Cần thay thế bằng module logging. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Reliability] | [src/engine/database.py:261] | Lý do: Bare except: block detected (Catches SystemExit/KeyboardInterrupt). AI Note: Đoạn mã sử dụng bare except:, điều này sẽ bắt cả SystemExit và KeyboardInterrupt, ngăn cản việc dừng chương trình một cách bình thường. Đây là một lỗi Reliability rõ rệt. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Reliability] | [src/engine/database.py:261] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Lỗi này tương tự vi phạm #0. Mặc dù có thể lập trình viên muốn bỏ qua các dòng dữ liệu lỗi, nhưng việc im lặng tuyệt đối không log lại lý do thất bại (như JSONDecodeError) là hành vi nguy hiểm cho tính toàn vẹn dữ liệu. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Reliability] | [src/engine/database.py:266] | Lý do: Bare except: block detected (Catches SystemExit/KeyboardInterrupt). AI Note: Tương tự vi phạm #2, việc bắt mọi exception mà không có định danh cụ thể (như Exception hoặc JSONDecodeError) là một anti-pattern trong Python, vi phạm nguyên tắc Reliability. | Trình bày: BARE_EXCEPT | Trọng số: -2.0
- [Maintainability] | [src/engine/ai_service.py:98] | Lý do: Function too long (God Object anti-pattern): deep_audit_batch (> 80 lines). AI Note: Việc một hàm xử lý logic (hàm deep_audit_batch) dài quá 80 dòng là vi phạm nguyên tắc 'Clean Code' và Single Responsibility Principle (SRP). Mặc dù thuật ngữ 'God Object' thường dùng cho Class, nhưng lỗi 'Function too long' ở đây là chính xác về mặt kỹ thuật duy trì mã nguồn (Maintainability). | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Maintainability] | [src/engine/ai_service.py:188] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng hàm 'print' trong các tầng Engine/Service của ứng dụng là một lỗi phổ biến. Trong môi trường production, cần sử dụng thư viện logging (như logging hoặc loguru) để có thể cấu hình log level, format và ghi log ra file hoặc các hệ thống giám sát tập trung. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/ai_service.py:247] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Tương tự như vi phạm #1, việc in thông báo lỗi retry ra stdout bằng lệnh 'print' làm mất tính linh hoạt của việc quản lý log. Lỗi này làm khó khăn cho việc debug trong môi trường không có console tương tác. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/ai_service.py:342] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Thông báo lỗi trong khối xử lý ngoại lệ (Exception) của quá trình Cross-Check bắt buộc phải dùng logger để đảm bảo tính nhất quán của hệ thống nhật ký vận hành. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/ai_service.py:337] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Kể cả các thông báo mang tính chất thông tin nghiệp vụ về kết quả xử lý (gỡ cờ False Positive) cũng nên được chuyển sang logger.info. Việc lạm dụng 'print' trong code service có thể làm ô nhiễm output của các processes khác hoặc CLI wrapper. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:413] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Việc sử dụng hàm print() trực tiếp trong các file xử lý logic (engine) là không tốt cho việc bảo trì. Trong môi trường production, nên sử dụng thư viện logging để có thể quản lý các mức độ log, tắt/bật dễ dàng và hỗ trợ ghi log ra file hoặc các hệ thống tập trung. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:414] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Tương tự như vi phạm trước, việc in kết quả kiểm toán trực tiếp ra console bằng print() trong mã nguồn ứng dụng khiến việc tích hợp hoặc chạy tự động (CI/CD) khó quản lý đầu ra hơn so với việc dùng logger. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:415] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Các dòng print này nằm trong logic của auditor.py. Việc thay thế bằng logger.info() sẽ chuyên nghiệp và linh hoạt hơn, cho phép cấu hình định dạng log đồng nhất trên toàn hệ thống. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:416] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Thông báo về xếp hạng và đường dẫn báo cáo là dữ liệu quan trọng, nên được ghi nhận thông qua hệ thống log chính thức thay vì print() đơn thuần. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:114] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Đây là một thông tin trạng thái (status update). Việc sử dụng print() để theo dõi tiến trình trong engine core có thể gây lộn xộn luồng đầu ra của các ứng dụng sử dụng engine này. Nên sử dụng logger.info(). | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Performance] | [src/engine/verification.py:78] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): load_rules (Complexity: 15 > 12). AI Note: Lỗi về độ phức tạp vòng cung (Cyclomatic Complexity) là một phép đo định lượng số lượng nhánh thực thi của hàm. Khi công cụ đo được 15 vượt quá ngưỡng cho phép là 12, đây là một lỗi thật về mặt hiệu suất và khả năng duy trì mã nguồn (Reliability/Performance). | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/verification.py:28] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng lệnh print() để thông báo lỗi trong code production là một thực hành không tốt (Bad practice). Nên sử dụng thư viện logging để có thể quản lý log level, format và định hướng output (file, stdout) tốt hơn. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/verification.py:55] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Tương tự vi phạm #2, việc sử dụng print() trong khối try-except tại src/engine/verification.py cần được thay thế bằng logging.error() hoặc logger.exception() để tuân thủ tiêu chuẩn code production. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/verification.py:197] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Việc in trực tiếp lỗi subprocess ra màn hình bằng lệnh print() không phù hợp với tiêu chuẩn code maintainability, đặc biệt là khi đi kèm các biểu tượng như emoji (❌) thường chỉ dùng cho CLI tool cơ bản, không nên dùng trong engine core. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Reliability] | [src/engine/verification.py:46] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Đây là một lỗi thật (True Positive). Việc sử dụng 'pass' trong khối ngoại lệ mà không log lỗi hoặc xử lý biến 'tree' sẽ dẫn đến lỗi 'NameError' ở các dòng mã tiếp theo nếu biến 'tree' được sử dụng, vì 'tree' sẽ không được khởi tạo nếu ast.parse thất bại. Ngay cả khi có ý định bỏ qua, việc không log lại khiến việc debug khi gặp file lỗi cú pháp trở nên khó khăn. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Maintainability] | [src/engine/scanners.py:83] | Lý do: Function too long (God Object anti-pattern): scan (> 80 lines). AI Note: Hàm `scan` được báo cáo dài hơn 80 dòng. Trong các hệ thống phân tích mã nguồn, các hàm thực hiện logic quét (scanning) với nhiều quy tắc thường dễ trở nên quá dài và khó bảo trì (God Object). Nếu không có bằng chứng mã nguồn cho thấy nó ngắn hơn, đây là lỗi thật. | Trình bày: GOD_OBJECT | Trọng số: -2.0
- [Performance] | [src/engine/scanners.py:83] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): scan (Complexity: 62 > 12). AI Note: Độ phức tạp Cyclomatic là 62, vượt xa ngưỡng khuyến nghị là 12. Điều này thường xảy ra trong các hàm `scan` có quá nhiều điều kiện logic lồng nhau hoặc các nhánh rẽ hướng xử lý cho từng loại rule, gây khó khăn cho việc testing và bảo trì. | Trình bày: HIGH_COMPLEXITY | Trọng số: -5.0
- [Maintainability] | [src/engine/auditor.py:69] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Việc sử dụng 'print' trực tiếp trong mã nguồn của engine (src/engine/) vi phạm nguyên tắc tách biệt logic và giao diện. Nên sử dụng thư viện logging để có thể quản lý log theo level, ghi vào file hoặc stream tùy cấu hình production. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Reliability] | [src/engine/auditor.py:81] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Việc nuốt lỗi 'ImportError' bằng lệnh 'pass' mà không có log cảnh báo hoặc comment giải thích là một lỗi về độ tin cậy (Reliability). Ngay cả khi đây là hành động có chủ đích (ví dụ: kiểm tra thư viện tùy chọn), hệ thống vẫn cần ghi log ở mức debug hoặc info để người vận hành biết tại sao một số tính năng có thể bị vô hiệu hóa. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Maintainability] | [src/engine/auditor.py:85] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Thông tin thống kê (LOC, total_files) nên được trả về trong một báo cáo cấu trúc hoặc ghi log thông qua logger thay vì in trực tiếp ra stdout trong lớp engine. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:86] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Tương tự như vi phạm #2, việc sử dụng các câu lệnh print rải rác trong mã nguồn logic gây khó khăn cho việc bảo trì và tùy biến đầu ra của ứng dụng. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/engine/auditor.py:89] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Các thông báo tiến trình (Progress updates) trong các bước xử lý của Engine nên được truyền tải qua logger hoặc cơ chế callback để lớp UI/CLI có thể xử lý hiển thị phù hợp. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Maintainability] | [src/api/api_server.py:16] | Lý do: Forgotten TODO/FIXME comment in code.. AI Note: Đoạn comment sử dụng từ khóa 'HACK' để đánh dấu một thay đổi mang tính chắp vá nhưng không đi kèm với lỗi/ticket cụ thể (như FIXME(PROJ-123)). Theo chỉ đạo, các ghi chú tạm thời và không có dấu hiệu được tracking như thế này được coi là vi phạm thực tế về Maintainability. | Trình bày: FORGOTTEN_TODO | Trọng số: -0.5
- [Maintainability] | [src/api/api_server.py:10] | Lý do: Import 'asyncio' might be unused (AST). AI Note: Thư viện 'asyncio' được import nhưng không có tham chiếu sử dụng trong các đoạn mã được cung cấp. Các công cụ phân tích tĩnh (AST) thường phát hiện chính xác các import thừa, gây lãng phí tài nguyên và làm giảm tính sạch sẽ của code. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Maintainability] | [src/api/api_server.py:17] | Lý do: Import 'starlette.formparsers' might be unused (AST). AI Note: Dấu hiệu từ vi phạm #0 cho thấy ý định sử dụng thư viện này, nhưng linter vẫn xác định nó là 'unused'. Điều này chứng tỏ người viết code mới chỉ import mà chưa thực sự viết logic cấu hình, khiến import này trở thành dư thừa tại thời điểm hiện tại. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Maintainability] | [src/engine/discovery.py:117] | Lý do: Print statement found in production code. Use logger instead.. AI Note: Sử dụng lệnh `print` trực tiếp trong mã nguồn của engine (`discovery.py`) thay vì sử dụng hệ thống logging là vi phạm quy tắc về khả năng bảo trì (Maintainability) trong môi trường production. | Trình bày: PRINT_STATEMENT | Trọng số: -0.5
- [Performance] | [tests/test_natural_rules_compiler.py:21] | Lý do: String concatenation using `+=` inside a loop is slow. Prefer `list.append()` and `''.join()`.. AI Note: Đây là lỗi thật (True Positive). Dựa trên tên biến 'chunk' và việc gọi 'sys.stdout.flush()', đoạn mã chắc chắn nằm trong một vòng lặp để thu thập dữ liệu từ stream. Việc nối chuỗi bằng toán tử `+=` trong vòng lặp là một anti-pattern về hiệu suất trong Python (độ phức tạp O(n^2)). Dù nằm trong tệp test, nó vẫn khớp chính xác với tiêu chí vi phạm được mô tả và chỉ đạo cụ thể (là Chuỗi và nằm trong Vòng lặp). | Trình bày: SLOW_STRING_CONCAT | Trọng số: -3.0
- [Reliability] | [src/engine/database.py:118] | Lý do: Hàm `get_history` (dòng 118) và `get_audit_by_id` (dòng 140) thực hiện `json.loads()` trực tiếp trên kết quả từ database mà không kiểm tra NULL. Nếu cột `pillar_scores` hoặc `full_json` trong PostgreSQL là NULL, `psycopg2` sẽ trả về `None`, dẫn đến `TypeError: the JSON object must be str... not 'NoneType'` và làm sập API. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/engine/auditor.py:223] | Lý do: Tại dòng 223, trọng số nhận từ AI được ép kiểu trực tiếp bằng `float(rv.get('weight'))`. Do dữ liệu từ AI Service là không thể đoán trước (untrusted input), nếu AI trả về một chuỗi không phải số (ví dụ: 'N/A', '-5,0'), hàm sẽ ném ra `ValueError` và làm dừng toàn bộ tiến trình kiểm toán đang chạy. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Performance] | [src/engine/database.py:153] | Lý do: Các hàm `save_project_rules` (dòng 153) và `toggle_core_rule` (dòng 183) sử dụng mô hình 'Select-then-Update/Insert'. Điều này gây ra dư thừa truy vấn (Redundant Query) và tạo ra Race Condition trong môi trường đa người dùng. Nên sử dụng cú pháp `INSERT ... ON CONFLICT (target_id) DO UPDATE` của PostgreSQL để tối ưu hiệu năng và đảm bảo tính nguyên tử. | Trình bày: REDUNDANT_DB_QUERY | Trọng số: -4.0
- [Maintainability] | [src/engine/discovery.py:17] | Lý do: Phương thức `generate_precheck_script` sử dụng String Interpolation (f-string) để tạo mã Python và thực thi qua `subprocess`. Đây là một lỗi thiết kế nghiêm trọng (Code Injection Sink). Nếu cấu hình `EXCLUDE_DIRS` chứa các ký tự đặc biệt như dấu nháy đơn `'`, script được tạo ra sẽ bị lỗi cú pháp hoặc tệ hơn là thực thi mã ngoài ý muốn. Nên import trực tiếp logic thay vì tạo file tạm. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Performance] | [src/engine/auditor.py:144] | Lý do: Trong bước 3.6 (AI Reasoning Audit), mã nguồn thực hiện `file_obj.read()` cho tất cả các file trong project và lưu vào danh sách `deep_chunks` trước khi xử lý. Với các dự án lớn (hàng trăm MB code), điều này gây ra nguy cơ tràn bộ nhớ (Out of Memory - OOM) do toàn bộ nội dung code của project bị nạp đồng thời vào RAM thay vì xử lý theo dạng stream hoặc generator. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/engine/authorship.py:105] | Lý do: Hàm `get_author_info` (dòng 99) chuyển đổi đường dẫn thành `rel_path` trước khi truy xuất cache, nhưng việc kiểm tra `if not file_path.startswith('/')` là không đủ tin cậy để làm key cho cache. Nếu cùng một file được tham chiếu bằng nhiều cách khác nhau (đường dẫn tương đối khác nhau), hệ thống sẽ chạy `git blame` nhiều lần cho cùng một file, gây hụt hiệu năng nghiêm trọng trên repo lớn. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Maintainability] | [src/api/routers/audit.py:95] | Lý do: Hàm 'cancel_audit' gọi 'AuditState.cancel()', nhưng logic thực thi trong 'run_auditor_with_capture' (auditor.run()) là đồng bộ và không có cơ chế kiểm tra tín hiệu hủy (interrupt/polling signal). Điều này khiến chức năng Hủy không có tác dụng thực tế cho đến khi quá trình quét tự kết thúc. | Trình bày: MISLEADING_NAME | Trọng số: -3.0
- [Maintainability] | [src/api/routers/audit.py:28] | Lý do: Sử dụng 'sys.stdout' redirection toàn cục trong một ứng dụng chuyển đổi bất đồng bộ (FastAPI) là cực kỳ nguy hiểm. Mặc dù có flag 'is_running', rủi ro Race Condition vẫn tồn tại nếu có các log từ system threads khác hoặc nếu nhiều job được quản lý không tốt. Việc ghi đè 'sys.stdout' ảnh hưởng đến toàn bộ Python process thay vì chỉ giới hạn trong ngữ cảnh của request/task hiện tại. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Reliability] | [src/api/routers/audit.py:121] | Lý do: Logic xác định 'target_path' trong 'upload_and_audit' dựa trên segment đầu tiên của file đầu tiên. Nếu người dùng upload một bộ file không nằm chung trong một thư mục gốc (ví dụ: upload trực tiếp các file trong src/ và tests/), auditor sẽ chỉ quét được một phần thư mục (ví dụ chỉ quét src/), dẫn đến kết quả kiểm toán bị thiếu hụt nghiêm trọng. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [src/engine/ai_service.py:257] | Lý do: Trong 'verify_flagged_issues', hệ thống tin tưởng tuyệt đối vào 'res.index' từ AI trả về. Nếu AI trả về index ngoài phạm vi mảng 'flagged_violations', hàm sẽ crash với lỗi IndexError. Cần có bước validate index trước khi truy cập mảng. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [src/api/routers/audit.py:133] | Lý do: Sau khi 'auditor.run()' kết thúc, mã nguồn truy cập trực tiếp 'auditor.discovery_data['total_loc']'. Nếu quá trình run() gặp lỗi nội bộ hoặc không khởi tạo được discovery_data, lệnh này sẽ gây crash (KeyError/AttributeError) cho Background Task, khiến job bị treo ở trạng thái không xác định thay vì log lỗi sạch sẽ. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -4.0
- [Performance] | [src/api/routers/rules.py:43] | Lý do: Mỗi lần gọi API '/rules' đều thực hiện đọc file 'rules.json' trực tiếp từ đĩa cứng. Với hệ thống có nhiều người dùng hoặc tần suất cấu hình cao, việc thiếu caching cho các luật mặc định này là một sự lãng phí tài nguyên IO không cần thiết. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/engine/natural_rules_compiler.py:80] | Lý do: Truy cập 'chunk.choices[0]' trong luồng streaming mà không kiểm tra độ dài mảng 'choices'. Trong một số trường hợp phản hồi từ API (như bị filter nội dung hoặc lỗi phần dẫn), 'choices' có thể rỗng, gây ra lỗi IndexError làm sập luồng xử lý luật. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/engine/verification.py:124] | Lý do: Thực hiện ép kiểu float() trực tiếp lên dữ liệu cấu hình bên ngoài (custom_weights) mà không có khối try/except. Nếu người dùng hoặc AI cung cấp một giá trị trọng số không hợp lệ (ví dụ: 'abc'), toàn bộ quá trình Audit sẽ bị crash (ValueError). | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/engine/verification.py:189] | Lý do: Logic trích xuất kết quả JSON từ stdout của subprocess (stdout_lines[-1]) cực kỳ mỏng manh. Nếu bất kỳ module nào được import hoặc chính mã nguồn đang được quét có lệnh print (ví dụ: log cảnh báo, DeprecationWarning), dòng cuối cùng của stdout sẽ không phải là JSON, gây ra lỗi phân tích cú pháp nghiêm trọng. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/engine/symbol_indexer.py:50] | Lý do: Sử dụng giá trị mặc định 'start + 15' khi không có end_lineno (Python < 3.8). Heuristic này khiến AI chỉ nhận được tối đa 15 dòng đầu của hàm/class, dẫn đến việc bỏ lỡ các lỗi nghiêm trọng nằm ở phần dưới của các hàm dài trong quá trình Verification. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [src/engine/scanners.py:149] | Lý do: Hàm kiểm tra 'missing_with_open' chỉ duyệt ngược tối đa 5 cấp node cha để tìm khối 'With'. Trong các cấu trúc code lồng nhau phức tạp (ví dụ: decorator lồng nhau hoặc generator expression phức tạp), việc giới hạn 'range(5)' sẽ gây ra các cảnh báo sai (False Positive). | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/config.py:31] | Lý do: Hàm 'int()' được thực hiện trực tiếp trên giá trị trả về từ 'os.getenv("TEST_MODE_LIMIT_FILES", 0)' mà không có khối try-except hoặc giá trị mặc định an toàn được kiểm tra trước. Nếu biến môi trường này bị cấu hình sai (vd: một chuỗi ký tự không phải số), ứng dụng sẽ gặp lỗi 'ValueError' và chết ngay lập tức khi load module configuration. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Maintainability] | [src/api/audit_state.py:64] | Lý do: Lớp 'AuditState' và các thuộc tính tĩnh như 'is_cancelled', 'is_running' mang tên gọi gây hiểu lầm nghiêm trọng. Mặc dù hệ thống đã chuyển sang mô hình đa nhiệm với 'JobManager', nhưng AuditState vẫn giữ các flag ở cấp độ Class (global). Điều này dẫn đến lỗi logic luồng xử lý: nếu một người dùng nhấn 'Cancel', tất cả các tiến trình kiểm toán của những người dùng khác đang chạy đồng thời trên server cũng sẽ bị hủy (Race condition/Global state conflict). | Trình bày: MISLEADING_NAME | Trọng số: -3.0
- [Reliability] | [src/api/audit_state.py:21] | Lý do: Biến 'legacy_logs' (và job_logs trong JobManager) là các List không có giới hạn (unbounded), tích lũy log của toàn bộ hệ thống từ lúc khởi động. Vì 'AuditLogHandler' trong api_server.py đẩy log liên tục vào các danh sách này mà không có cơ chế dọn dẹp hoặc xoay vòng (log rotation), nó sẽ gây ra lỗi rò rỉ bộ nhớ (Memory Leak) khiến hệ thống crash sau thời gian dài vận hành. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Security] | [src/api/git_helper.py:69] | Lý do: Cơ chế lọc thông tin nhạy cảm trong log lỗi bị hổng. Bạn sử dụng '.replace(token, "***")' để giấu App Password, nhưng trước đó token đã được URL-encoded bằng 'urllib.parse.quote(token)'. Nếu token chứa các ký tự đặc biệt (ví dụ: #, @, :), chuỗi xuất hiện trong lỗi của Git (thường là URL thô) sẽ ở dạng encoded và phép so sánh chuỗi nguyên bản sẽ thất bại, dẫn đến việc lộ token thật vào log hệ thống. | Trình bày: AI_REASONING | Trọng số: -6.0
- [Reliability] | [src/api/api_server.py:86] | Lý do: Middleware 'add_private_network_header' thực hiện can thiệp thủ công vào header CORS và chặn luồng 'OPTIONS' một cách tùy tiện trước khi tới 'CORSMiddleware'. Việc gán cứng 'Access-Control-Allow-Headers' = '*' khi 'allow_credentials=True' là một cấu hình không hợp lệ theo đặc tả CORS của các trình duyệt hiện đại, gây ra lỗi kết nối từ Frontend mặc dù server vẫn báo 200. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [tests/test_natural_rules_compiler.py:51] | Lý do: The database query 'AuditDatabase.get_project_rules' is called and its result 'res' is immediately accessed as a dictionary (res['natural_text']) without any null-check. If the rule is not found in the database, 'res' will likely be None, leading to a TypeError at runtime. This matches the UNCHECKED_NONE_RETURN pattern. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [tests/test_authorship.py:57] | Lý do: The call to 'tracker.get_author_info' may return None if the file is not recognized by the git engine or if the line number is out of bounds. The code fails to check if 'info1' is None before accessing 'info1['boundary']', risking a crash during test execution. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [tests/test_natural_rules_compiler.py:36] | Lý do: Fragile logic for 'repairing' AI-generated JSON. The code blindly wraps the content in curly braces if it doesn't start with '{'. This heuristic is unsafe as it makes dangerous assumptions about the structure of the LLM output (e.g., it might be a list or a string that was simply not JSON), which can lead to unpredictable parsing behavior in the subsequent json.loads call. | Trình bày: AI_REASONING | Trọng số: -4.0
