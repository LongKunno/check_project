# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Maintainability] | [src/engine/ai_service.py:7] | Lý do: Import 'ValidationError' might be unused (AST). AI Note: Dựa trên phân tích AST, 'ValidationError' được import từ pydantic nhưng không được sử dụng trong logic của file. Đây là một lãng phí tài nguyên và làm giảm tính Maintainability. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Performance] | [src/engine/dependency_checker.py:12] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): detect_circular_dependencies (Complexity: 21 > 20). AI Note: Chỉ số Cyclomatic Complexity là 21, vượt quá ngưỡng cho phép (20). Điều này cho thấy hàm 'detect_circular_dependencies' đang xử lý quá nhiều điều kiện logic lồng nhau, gây khó khăn cho việc bảo trì và kiểm thử. | Trình bày: HIGH_COMPLEXITY | Trọng số: -2.0
- [Reliability] | [src/engine/dependency_checker.py:48] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Đoạn mã sử dụng 'except Exception as e: pass' để nuốt mọi lỗi xảy ra mà không ghi log hay xử lý lại. Đây là một lỗi Reliability nghiêm trọng vì nó có thể che giấu các lỗi runtime không mong muốn như KeyError hoặc TypeError. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Maintainability] | [src/engine/dependency_checker.py:7] | Lý do: Import 'logging' might be unused (AST). AI Note: Thư viện 'logging' được import nhưng không xuất hiện trong các lời gọi hàm phía dưới. Đây là lỗi import thừa theo kết quả phân tích AST. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Performance] | [src/engine/scanners.py:89] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): scan (Complexity: 24 > 20). AI Note: Độ phức tạp Cyclomatic (Cyclomatic Complexity - CC) đạt mức 24, vượt qua ngưỡng cho phép là 20. Đây là một lỗi thật (True Positive) vì trong các engine quét mã, hàm 'scan' thường là hàm điều phối chính. Khi hàm này xử lý quá nhiều logic (vòng lặp duyệt rules, duyệt file, các điều kiện rẽ nhánh cho từng loại cây AST), nó trở nên cực kỳ khó bảo trì và kiểm thử. Mặc dù được xếp vào trụ cột Performance, nhưng CC cao thường tỷ lệ thuận với việc thực thi nhiều chỉ thị rẽ nhánh trong bytecode, có thể làm chậm quá trình quét khi xử lý codebase lớn. | Trình bày: HIGH_COMPLEXITY | Trọng số: -2.0
- [Performance] | [src/engine/scanners.py:187] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): _check_calls (Complexity: 24 > 20). AI Note: Vi phạm này là lỗi thật (True Positive). Phương thức '_check_calls' có CC là 24, cho thấy nó đang chứa quá nhiều logic lồng nhau hoặc các chuỗi if-elif dày đặc để kiểm tra các loại node (ast.Name, ast.Attribute) và các tham số hàm. Trong phân tích tĩnh, việc gộp quá nhiều logic kiểm tra vào một hàm duy nhất không chỉ gây khó khăn cho việc bảo trì mà còn ảnh hưởng đến hiệu suất thực thi do chi phí kiểm tra điều kiện liên tục. Phương thức này nên được tách nhỏ thành các hàm phụ trợ hoặc sử dụng pattern Visitor chuyên biệt. | Trình bày: HIGH_COMPLEXITY | Trọng số: -2.0
- [Reliability] | [src/api/audit_state.py:86] | Lý do: Sử dụng Class Variables (is_cancelled, is_running) để quản lý trạng thái trong môi trường đa luồng (FastAPI/Uvicorn) dẫn đến lỗi logic nghiêm trọng. Nếu một Request gọi AuditState.cancel(), toàn bộ các tiến trình Audit khác của tất cả người dùng khác sẽ bị dừng do dùng chung vùng nhớ Class-level. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Reliability] | [src/api/api_server.py:144] | Lý do: Ép kiểu int() trực tiếp từ biến môi trường OS (PORT) mà không có khối try/except bảo vệ. Nếu giá trị PORT bị cấu hình sai (vd: 'abc'), server sẽ crash ngay khi khởi động. Đây là lỗi Unsafe type coercion trên input bên ngoài. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/api/audit_state.py:38] | Lý do: Race Condition & Data Loss: Hàm JobManager.create_job thực hiện cls.legacy_logs.clear() mỗi khi có một Job mới được tạo. Trong môi trường API có nhiều người dùng cùng lúc, việc khởi tạo Job A sẽ xóa sạch toàn bộ log đang hiển thị của Job B nếu hai Job này bắt đầu gần nhau. | Trình bày: AI_REASONING | Trọng số: -6.0
- [Security] | [src/api/git_helper.py:70] | Lý do: Việc xử lý che giấu Token trong lỗi Git chỉ sử dụng .replace(token, '***') là không triệt để. Nếu URL chứa credentials được định dạng theo cách khác hoặc token bị URL-encoded trong output của Git, thông tin nhạy cảm vẫn có thể bị leak qua Exception message trả về cho API client. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Maintainability] | [src/api/api_server.py:21] | Lý do: Monkey-patching Starlette's MultiPartParser trực tiếp trong API entry point là một kỹ thuật rủi ro cao (Fragile Monkeypatch). Nếu phiên bản Starlette/FastAPI cập nhật cấu trúc __init__, ứng dụng sẽ lỗi runtime mà không có cảnh báo rõ ràng từ Static Analysis. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [src/engine/database.py:236] | Lý do: Hàm 'get_project_rules' sử dụng 'cursor.fetchone()' nhưng không kiểm tra None trước khi truy cập các key như 'row['natural_text']'. Nếu 'target_id' không tồn tại trong DB, 'row' sẽ là None và gây ra 'TypeError: 'NoneType' object is not subscriptable'. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Performance] | [src/engine/database.py:28] | Lý do: Kiến trúc Database không sử dụng Connection Pooling. Mọi thao tác (save_audit, get_history, save_project_rules) đều gọi 'get_connection()' để tạo một kết nối vật lý mới và đóng ngay sau đó. Trong môi trường kiểm toán mã nguồn lớn với nhiều tiến trình asyncio chạy song song, việc này sẽ gây quá tải CPU/RAM của DB server và dễ dẫn đến lỗi 'Too many connections'. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [src/engine/discovery.py:43] | Lý do: Sử dụng 'os.walk' với tham số 'followlinks=True' mà không có cơ chế kiểm soát độ sâu hoặc kiểm tra vòng lặp symlink. Nếu thư mục mục tiêu chứa các symbolic link trỏ ngược lại thư mục cha (circular symlinks), trình quét sẽ rơi vào trạng thái đệ quy vô hạn dẫn đến treo hệ thống hoặc tràn bộ nhớ. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Reliability] | [src/engine/dependency_checker.py:43] | Lý do: Logic phát hiện phụ thuộc vòng quá sơ sài khi chỉ lấy phần đầu của package ('n.name.split('.')[0]'). Nếu 'module_a' và 'module_b' cùng nằm trong gói 'src.engine', cả hai sẽ được coi là 'src', dẫn đến việc bỏ lỡ các quan hệ phụ thuộc vòng thực sự giữa các sub-modules hoặc gây ra cảnh báo giả (False Positive) cho chính package đó. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/engine/discovery.py:21] | Lý do: Mã nguồn thực hiện việc sinh code Python vào một file tạm ('ai_precheck.py') rồi thực thi bằng 'subprocess'. Đây là một anti-pattern (Meta-programming nguy hiểm), gây khó khăn cho việc debug, vi phạm các chính sách an toàn thực thi (W^X) và tiềm ẩn nguy cơ Code Injection nếu các biến cấu hình (EXCLUDE_DIRS) không được sanitize kỹ. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Performance] | [src/engine/auditor.py:179] | Lý do: Trong '_build_deep_audit_batches', toàn bộ nội dung của các file được đọc vào bộ nhớ ('fobj.read()') để tính toán dung lượng batch. Mặc dù có giới hạn 'MAX_CHARS', nhưng nếu dự án có nhiều file cực lớn (vd: dữ liệu test, file nén giả dạng text), việc load cùng lúc có thể gây spike RAM đột ngột trước khi kịp phân nhóm. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/audit.py:33] | Lý do: Global process-level state pollution in 'run_auditor_with_capture'. Reassigning 'sys.stdout' to a custom stream is not thread-safe in a concurrent FastAPI environment. Multiple audit jobs will overwrite the global 'sys.stdout', causing 'Log Bleeding' where logs from one job are routed to the SSE stream of a different job. Furthermore, restoring 'sys.stdout' in a 'finally' block will prematurely stop logging for other concurrent tasks. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Reliability] | [src/api/routers/audit.py:111] | Lý do: Logic flaw in 'upload_and_audit' directory reconstruction. The 'project_name' variable is reassigned for every file in the loop: 'project_name = safe_parts[0]'. If a user uploads multiple files from different root directories (e.g., 'src/api.py' and 'tests/test.py'), 'project_name' will only reflect the root of the last file processed. Consequently, 'target_path' will target a subdirectory, causing the Auditor to ignore all other uploaded folders. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Security] | [src/api/routers/audit.py:147] | Lý do: The legacy '/audit' endpoint is vulnerable to Local File/Directory Information Disclosure. It accepts an arbitrary 'target' string and calls 'os.path.abspath(target)' without validation or sandboxing. An attacker could scan sensitive system directories (e.g., /etc, /root) by triggering an audit on those paths. | Trình bày: AI_REASONING | Trọng số: -8.0
- [Performance] | [src/api/routers/rules.py:54] | Lý do: Redundant I/O and lack of caching in 'get_rules'. The 'rules.json' file is read and parsed from the filesystem on every GET request. This is inefficient for a static engine configuration and should be loaded into memory once during application startup or after a rule update. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/rules.py:41] | Lý do: Incomplete error handling in '_run_scanners_sync'. If 'ast.parse(content)' fails with a SyntaxError, the 'tree' variable becomes 'None'. However, 'tree' is still passed to 'PythonASTScanner().scan()'. If the scanner implementation lacks a null-check for the AST tree, it will result in an 'AttributeError' or crash during the audit loop. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/api/routers/audit.py:95] | Lý do: The '/audit/cancel' endpoint provides misleading feedback. It returns a success message saying the signal was sent, but 'auditor.run()' (called inside the background task) is a blocking synchronous call. There is no mechanism within 'run_auditor_with_capture' or 'CodeAuditor' shown to check the 'AuditState.cancel' signal and stop the process, meaning the audit will likely continue until completion regardless of the cancel request. | Trình bày: MISLEADING_NAME | Trọng số: -3.0
- [Reliability] | [src/api/routers/audit.py:105] | Lý do: Race condition in execution locking. The check 'if AuditState.is_running: raise HTTPException(409)' is performed at the router level, but 'AuditState.is_running' is only set to 'True' once the background task starts. This allows multiple requests to bypass the lock and queue multiple concurrent audits in the background worker, violating the intended design of 'one audit at a time'. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Maintainability] | [tests/test_overall_pillars.py:64] | Lý do: Các đoạn ghi chú (comments) giải thích logic và kết quả tính toán điểm số (Pillar Score) (ví dụ: dòng 58-62 đề cập kết quả là 1.67 và 3.33) mâu thuẫn trực tiếp với các giá trị được khẳng định bằng `assert` trong mã nguồn (8.18 và 7.34). Sự mâu thuẫn giữa tài liệu/chú thích và mã thực thi là biểu hiện của 'Misleading' và gây khó khăn cho việc bảo trì. | Trình bày: MISLEADING_NAME | Trọng số: -1.0
- [Maintainability] | [tests/test_authorship.py:28] | Lý do: Sử dụng tham số `--date="1 year ago"` trong danh sách các đối số của `subprocess.run` mà không thông qua shell (`shell=True`). Git sẽ nhận được giá trị ngày bao gồm cả dấu ngoặc kép literal (chuỗi '"1 year ago"'), điều này có thể khiến Git không phân giải được thời gian chính xác như mong đợi của test case. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Maintainability] | [tests/test_natural_rules_compiler.py:28] | Lý do: Logic xử lý chuỗi JSON `if not json_str.startswith('{'): json_str = '{' + json_str + '}'` quá đơn giản và rủi ro. Nếu đầu ra của AI là một mảng JSON `[...]`, việc tự ý thêm ngoặc nhọn `{` ở đầu sẽ tạo ra một chuỗi JSON không hợp lệ, gây lỗi `json.loads` sau đó. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [tests/test_authorship.py:11] | Lý do: Sử dụng đường dẫn cứng `/tmp/test_authorship_project` cho thư mục làm việc của test. Điều này gây rủi ro Race Condition nếu nhiều tiến trình test chạy đồng thời trên cùng một máy, hoặc lỗi quyền truy cập nếu thư mục đã tồn tại và thuộc sở hữu của user khác. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [/src/engine/natural_rules_compiler.py:89] | Lý do: Truy cập trực tiếp chunk.choices[0] mà không kiểm tra độ dài mảng 'choices'. Trong một số trường hợp stream bị lỗi hoặc trả về rỗng từ API AI, việc này sẽ gây lỗi IndexError tại runtime. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Performance] | [/src/engine/scanners.py:70] | Lý do: Sử dụng _pattern_cache làm class variable để lưu trữ Regex pattern mà không có cơ chế giới hạn dung lượng hoặc dọn dẹp (eviction policy). Nếu hệ thống chạy lâu dài với hàng nghìn luật do AI sinh ra, điều này sẽ gây rò rỉ bộ nhớ (Memory Leak). | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [/src/engine/verification.py:216] | Lý do: Phương pháp lấy kết quả phân tích bằng stdout_lines[-1] rất không an toàn. Nếu kịch bản verify in ra bất kỳ cảnh báo nào (warnings) hoặc thông tin debug sau khi khối JSON được in, việc gọi json.loads trên dòng cuối cùng sẽ thất bại. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [/src/engine/scanners.py:140] | Lý do: Việc gán child.parent bằng cách duyệt lồng (nested loop) trong scan() gây lãng phí tài nguyên O(N) mỗi khi scan một file. Ngoài ra, logic _check_calls dựa vào thuộc tính 'parent' tự gán này để kiểm tra __main__ guard; nếu một node AST bị biến đổi trước đó, thuộc tính này có thể bị mất dẫn đến kết quả sai. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [tests/test_authorship.py:43] | Lý do: Hàm `get_author_info` có khả năng trả về `None` nếu tệp không được Git theo dõi hoặc chỉ số dòng không hợp lệ, nhưng kết quả `info1` được truy cập trực tiếp (`info1['boundary']`) mà không có bước kiểm tra `None`. Điều này có thể dẫn đến lỗi `TypeError` khi chạy test. [Cross-Checked: Mặc dù hệ thống không tìm thấy định nghĩa hàm get_author_info để đối chiếu, sự nghi ngờ ban đầu về việc truy cập 'info1["boundary"]' mà không kiểm tra 'None' vẫn có cơ sở logic. Trong các thao tác Git, nếu tệp không tồn tại hoặc chưa được theo dõi, việc trả về 'None' là rất phổ biến. Vì không có bằng chứng chứng minh hàm này luôn trả về một dictionary, rủi ro gây TypeError trong kịch bản test vẫn tồn tại.] | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [tests/test_discovery.py:56] | Lý do: Kết quả trả về từ `discovery1.run_discovery()` được sử dụng trực tiếp để truy cập thuộc tính `['features']` mà không kiểm tra xem biến `report1` có phải là `None` hay không. Điều này tiềm ẩn rủi ro crash nếu quá trình discovery thất bại. [Cross-Checked: Tương tự như trường hợp trên, việc thiếu bằng chứng về định nghĩa của run_discovery() khiến ta không thể khẳng định hàm này được thiết kế để không bao giờ trả về 'None'. Trong quy trình discovery (thường liên quan đến I/O hoặc quét tệp tin), khả năng thất bại là có và việc sử dụng trực tiếp kết quả để truy cập key mà không kiểm tra an toàn là vi phạm nguyên tắc Reliability.] | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [/src/engine/verification.py:178] | Lý do: Hàm generate_verification_script sử dụng inspect.getsource để trích xuất mã nguồn của detect_circular_dependencies từ module khác. Điều này không sao chép các 'import' cần thiết hoặc biến toàn cục bên trong module dependency_checker, dẫn đến lỗi NameError khi chạy kịch bản ai_double_check.py trong một process mới. [Cross-Checked: Bằng chứng mã nguồn tại src/engine/dependency_checker.py xác nhận sự nghi ngờ là chính xác. Hàm detect_circular_dependencies sử dụng các thư viện 'os' và 'ast' (dòng 23, 40, 41, v.v.) vốn được import ở cấp độ module. Khi generate_verification_script sử dụng inspect.getsource, nó chỉ trích xuất phần thân hàm. Do các câu lệnh import này không nằm bên trong hàm, kịch bản được tạo ra khi chạy độc lập sẽ gây lỗi NameError vì thiếu các module cần thiết.] | Trình bày: AI_REASONING | Trọng số: -7.0
