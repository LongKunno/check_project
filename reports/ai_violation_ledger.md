# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Reliability] | [src/api/audit_state.py:105] | Lý do: Sử dụng Shared Global State cho dữ liệu log. Class legacy 'AuditState' sử dụng 'logs = []' làm biến lớp (Class Attribute). Trong môi trường đa luồng của FastAPI, danh sách này được dùng chung cho mọi instance và mọi request. Việc gọi 'cls.logs.clear()' hoặc append() từ một tiến trình kiểm toán sẽ gây hỏng dữ liệu hoặc mất log của các tiến trình đang chạy song song khác. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Security] | [src/api/api_server.py:19] | Lý do: Mở rộng bề mặt tấn công DoS qua Multipart upload. Việc monkey-patching 'MultiPartParser' để tăng 'max_files' và 'max_fields' lên tới 10,000 (mặc định của Starlette là 1,000) cho phép kẻ tấn công gửi các request cực lớn gây cạn kiệt tài nguyên bộ nhớ (RAM) hoặc file descriptors của hệ thống, trái ngược hoàn toàn với mục đích 'hạn chế DoS' được nêu trong comment. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/api_server.py:113] | Lý do: Xử lý ngoại lệ Validation sai chuẩn HTTP. 'validation_exception_handler' trả về một dictionary thay vì 'JSONResponse' có kèm 'status_code=422'. Điều này khiến FastAPI trả về mã trạng thái '200 OK' cho cả các trường hợp dữ liệu đầu vào không hợp lệ, gây nhầm lẫn cho logic xử lý ở frontend và vi phạm thiết kế chuẩn RESTful API. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/git_helper.py:86] | Lý do: Tiềm ẩn lỗi Logic khi lọc thông báo lỗi nhạy cảm. 'error_msg = str(e.stderr)' có thể trả về đại diện chuỗi của bytes (ví dụ: "b'error content'") thay vì nội dung thực tế nếu 'e.stderr' là kiểu bytes. Điều này có thể khiến Regex lọc token không khớp được chuỗi cần xóa, dẫn đến rò rỉ token bí mật vào log hệ thống. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/engine/auditor.py:216] | Lý do: Lỗi runtime (AttributeError): Trong phương thức '_log_ai_violations', mã nguồn cố gắng truy cập 'self.rules.get('rules', [])'. Tuy nhiên, thuộc tính 'self.rules' không được định nghĩa hoặc khởi tạo trong hàm '__init__' của lớp CodeAuditor (chỉ có 'self.custom_rules'). Điều này sẽ khiến ứng dụng bị crash ngay lập tức khi xử lý các vi phạm do AI phát hiện. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/engine/database.py:105] | Lý do: Rò rỉ kết nối Database (Connection Leak): Các phương thức như 'save_audit', 'get_history', 'get_audit_by_id', 'save_project_rules' và 'save_custom_weights' lấy kết nối từ pool bằng 'get_connection()' nhưng không sử dụng khối 'try...finally'. Nếu một câu lệnh SQL thất bại hoặc 'json.loads' gặp lỗi, 'release_connection' sẽ không bao giờ được gọi, dẫn đến cạn kiệt pool kết nối trong môi trường production. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Security] | [src/engine/discovery.py:22] | Lý do: Rủi ro Code Injection tiềm ẩn: Phương thức 'generate_precheck_script' sử dụng f-string để nhúng trực tiếp nội dung của 'EXCLUDE_DIRS' và 'SCAN_EXTENSIONS' vào một file Python tạm thời sau đó thực thi file này bằng 'subprocess'. Nếu các cấu hình này bị thay đổi từ bên ngoài (ví dụ qua file config độc hại), nó cho phép thực thi mã Python tùy ý trên hệ thống. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Maintainability] | [src/engine/auditor.py:26] | Lý do: Vi phạm thiết kế 'God Object': Lớp CodeAuditor đang đảm nhận quá nhiều trách nhiệm trái ngược với nguyên lý Single Responsibility. Nó quản lý từ việc khám phá tài nguyên, thực thi quét, điều phối AI, đến tính toán điểm số (aggregation) và ghi báo cáo trực tiếp vào file. Điều này làm tăng độ phức tạp khi cần bảo trì hoặc thay đổi logic của từng bước riêng biệt. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Performance] | [src/engine/database.py:49] | Lý do: Chặn luồng khởi tạo (Blocking Initialization): Phương thức 'initialize' trong AuditDatabase thực hiện vòng lặp retry 10 lần với 'time.sleep(2)' ngay trong luồng chính khi cố gắng kết nối Postgres. Điều này có thể khiến ứng dụng bị treo (freeze) lên đến 20 giây khi bắt đầu audit nếu database đang khởi động hoặc gặp sự cố mạng. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/audit.py:105] | Lý do: Sử dụng 'AuditState' (logs, is_running) như một đối tượng global duy nhất cho toàn bộ ứng dụng. Khi nhiều người dùng thực hiện yêu cầu quét mã nguồn cùng lúc, hàm 'AuditState.reset()' được gọi ở đầu mỗi router sẽ xóa sạch log của các phiên làm việc khác đang chạy, dẫn đến việc mất dữ liệu log thời gian thực hoặc hiển thị sai trạng thái cho người dùng khác. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/audit.py:108] | Lý do: Trong endpoint '/audit/process', biến 'project_name' bị gán cứng giá trị 'uploaded_project' khi khởi tạo JobManager. Trong môi trường đa người dùng, việc sử dụng một mã định danh tĩnh cho các Job upload khác nhau sẽ gây ra xung đột (collision) trong hệ thống quản lý tác vụ, dẫn đến việc ghi đè trạng thái ('RUNNING'/'COMPLETED') và log giữa các phiên upload khác nhau. | Trình bày: AI_REASONING | Trọng số: -1.8
