# SỔ CÁI VI PHẠM (AI VIOLATION LEDGER)

- [Performance] | [src/engine/scanners.py:89] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): scan (Complexity: 24 > 20). AI Note: Hàm scan đóng vai trò điều phối chính trong engine scanner, thường chứa nhiều vòng lặp duyệt qua các quy tắc (rules) và các điều kiện kiểm tra cây AST. Chỉ số Cyclomatic Complexity (CC) là 24 vượt qua ngưỡng khuyến nghị thông thường (thường là 10 hoặc 20), gây khó khăn cho việc bảo trì và kiểm thử đơn vị (unit testing). Mặc dù nó được phân loại vào trụ cột 'Performance', nhưng độ phức tạp cao thường dẫn đến hiệu năng kém do quá trình rẽ nhánh quá nhiều. | Trình bày: HIGH_COMPLEXITY | Trọng số: -2.0
- [Performance] | [src/engine/scanners.py:187] | Lý do: Cyclomatic Complexity too high (Too many nested loops/branches): _check_calls (Complexity: 24 > 20). AI Note: Hàm _check_calls thường chứa các logic phức tạp để phân tích các hàm gọi đến (function calls), bao gồm việc xử lý các dạng node khác nhau (ast.Name, ast.Attribute) và kiểm tra đối chiếu với danh sách dangerous_funcs. Việc đạt chỉ số CC 24 cho thấy hàm này đang đảm nhận quá nhiều logic kiểm tra lồng nhau, cần được tách nhỏ thành các hàm bổ trợ để giảm tải độ phức tạp và cải thiện khả năng đọc hiểu. | Trình bày: HIGH_COMPLEXITY | Trọng số: -2.0
- [Maintainability] | [src/api/routers/audit.py:8] | Lý do: Import 'io' might be unused (AST). AI Note: Import 'io' được khai báo nhưng không được sử dụng trong mã nguồn. Đây là một vi phạm về Maintainability (unused import) thực tế. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Maintainability] | [src/engine/ai_service.py:7] | Lý do: Import 'ValidationError' might be unused (AST). AI Note: ValidationError được import từ pydantic nhưng không được dùng để bắt lỗi hay kiểm tra kiểu trong đoạn mã. Đây là True Positive. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Reliability] | [src/engine/dependency_checker.py:60] | Lý do: Swallowed exception (except block has only 'pass'). AI Note: Việc sử dụng 'except Exception as e: pass' mà không có bất kỳ dòng log hay ghi chú giải thích nào là một lỗi Reliability (Swallowed exception). Nó che giấu mọi lỗi phát sinh (bao gồm cả lỗi không mong muốn) và gây khó khăn cho việc debug. Vì không có comment cho thấy đây là hành vi cố ý được kiểm soát, nên đây là True Positive. | Trình bày: SWALLOWED_EXCEPTION | Trọng số: -5.0
- [Maintainability] | [src/engine/dependency_checker.py:7] | Lý do: Import 'logging' might be unused (AST). AI Note: Thư viện 'logging' được import nhưng không được sử dụng. Đặc biệt là trong Vi phạm #2, nếu logging được sử dụng để ghi lại lỗi thay vì 'pass', vi phạm này đã không xảy ra. Đây là lỗi Maintainability thực tế. | Trình bày: UNUSED_IMPORT | Trọng số: -0.5
- [Reliability] | [src/api/audit_state.py:30] | Lý do: Sử dụng 'threading.local()' để lưu trữ 'active_job_id' trong môi trường ASGI (FastAPI) là không an toàn. FastAPI chạy trên event loop (asyncio), nơi nhiều coroutine xử lý các request khác nhau có thể chạy trên cùng một thread. Việc sử dụng threading.local sẽ khiến dữ liệu job_id bị nhiễm chéo (cross-contamination) giữa các request concurrent. Cần sử dụng 'contextvars' để đảm bảo context-safety. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Reliability] | [src/api/audit_state.py:85] | Lý do: Lớp 'AuditState' sử dụng các biến class (is_cancelled, is_running, logs) để quản lý trạng thái. Trong một ứng dụng Web đa người dùng, các biến này được chia sẻ giữa toàn bộ ứng dụng. Nếu hai người dùng thực hiện audit cùng lúc, họ sẽ ghi đè trạng thái của nhau, dẫn đến race condition và dữ liệu log bị trộn lẫn. Đây là lỗi thiết kế 'Global State' nghiêm trọng. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Performance] | [src/api/audit_state.py:88] | Lý do: Biến 'AuditState.logs' (list) tích lũy log từ 'AuditLogHandler' mỗi khi không có job nào active. Tuy nhiên, không có cơ chế giới hạn kích thước hoặc dọn dẹp (cleanup) cho danh sách này trừ khi gọi 'reset()' thủ công. Điều này sẽ gây ra hiện tượng rò rỉ bộ nhớ (Memory Leak) theo thời gian khi ứng dụng chạy liên tục. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Security] | [src/api/api_server.py:18] | Lý do: Việc ghi đè cấu hình Starlette MultiPartParser để tăng 'max_files' và 'max_fields' lên 10,000 (mặc định là 1000) tạo ra rủi ro bị tấn công từ chối dịch vụ (DoS). Kẻ tấn công có thể gửi các payload multipart cực lớn chứa hàng ngàn file/field nhỏ để làm cạn kiệt tài nguyên CPU và RAM của server khi phân tích headers. | Trình bày: AI_REASONING | Trọng số: -6.0
- [Reliability] | [src/api/api_server.py:126] | Lý do: Thực hiện ép kiểu 'int()' trực tiếp lên giá trị lấy từ biến môi trường 'PORT' mà không có khối try/except hoặc giá trị fallback an toàn tại thời điểm chạy. Nếu 'PORT' được cấu hình sai (vd: một chuỗi không phải số), ứng dụng sẽ crash ngay khi khởi động. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/engine/database.py:105] | Lý do: Trong các phương pháp `save_audit`, `get_history`, `get_audit_by_id`, `save_project_rules` và `save_custom_weights`, kết nối database (`conn`) được mở nhưng không được đóng trong khối `finally`. Nếu một truy vấn SQL gặp lỗi (Exception), `conn.close()` sẽ bị bỏ qua, dẫn đến rò rỉ kết nối (Connection Leak). Theo thời gian, điều này sẽ làm cạn kiệt pool kết nối của PostgreSQL và khiến hệ thống treo. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Reliability] | [src/engine/database.py:143] | Lý do: Hàm `get_history` (dòng 143) và `get_audit_by_id` (dòng 173) sử dụng `json.loads(d.get('pillar_scores', '{}'))`. Trong PostgreSQL, nếu cột `pillar_scores` có giá trị `NULL`, `RealDictCursor` sẽ trả về giá trị Python là `None`. Phương thức `d.get()` với key tồn tại nhưng value là `None` sẽ trả về `None`, dẫn đến `json.loads(None)` gây ra lỗi `TypeError` và làm sập ứng dụng. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Security] | [src/engine/discovery.py:16] | Lý do: Lớp `DiscoveryStep` tạo ra một file Python tạm `ai_precheck.py` ngay trong thư mục nguồn của khách hàng (`target_dir`) và thực thi nó. Đây là một hành vi xâm nhập không an toàn (Intrusive). Ngoài ra, việc nhúng các cấu hình như `EXCLUDE_DIRS` trực tiếp vào mã script thông qua f-string mà không thông qua cơ chế serialization an toàn tiềm ẩn nguy cơ Code Injection nếu các biến cấu hình này bị thao túng. | Trình bày: AI_REASONING | Trọng số: -6.0
- [Maintainability] | [src/engine/auditor.py:43] | Lý do: Toàn bộ các thao tác ghi file báo cáo (`ledger_path`, `report_path`, `ai_audit_report.json`) không chỉ định tham số `encoding='utf-8'`. Trên các hệ thống có locale mặc định không phải UTF-8 (như Windows), việc đọc/ghi mã nguồn hoặc báo cáo có chứa ký tự tiếng Việt hoặc ký tự đặc biệt sẽ gây lỗi `UnicodeDecodeError` hoặc làm sai lệch dữ liệu báo cáo. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/engine/database.py:214] | Lý do: Hàm `toggle_core_rule` có nguy cơ Race Condition. Mặc dù sử dụng `SELECT ... FOR UPDATE` để khóa hàng, nhưng nếu `target_id` chưa tồn tại trong bảng `project_rules`, khóa này sẽ không có tác dụng. Hai tiến trình chạy song song có thể cùng thấy không có hàng và cùng thực hiện `INSERT`, dẫn đến lỗi `UniqueViolation` tại tầng database cho cột `target_id`. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Maintainability] | [src/engine/dependency_checker.py:28] | Lý do: Cấu trúc map của `_build_mappings` sử dụng `os.path.basename` làm key định danh mô-đun (`short_name`). Nếu dự án có hai file trùng tên ở các thư mục khác nhau (vd: `auth/utils.py` và `db/utils.py`), file sau sẽ ghi đè file trước trong `mod_to_f`. Điều này dẫn đến việc phát hiện phụ thuộc vòng bị sai lệch hoặc báo cáo nhầm file. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [src/engine/verification.py:173] | Lý do: Hàm 'generate_verification_script' sử dụng `inspect.getsource` để tạo mã nguồn cho script chạy subprocess. Tuy nhiên, hàm `double_check_modular` bên trong script này tham chiếu đến biến global `logger` (được định nghĩa ở cấp module trong verification.py) nhưng biến này không được định nghĩa hoặc import trong script mới được tạo ra. Việc này sẽ dẫn đến lỗi `NameError: name 'logger' is not defined` khi thực hiện bước Verification. | Trình bày: AI_REASONING | Trọng số: -7.0
- [Reliability] | [src/engine/verification.py:140] | Lý do: Trong phương thức '_merge_custom_payload', mã nguồn ánh xạ các luật Regex từ AI mà không kiểm tra tính hợp lệ của chuỗi `pattern`. Nếu mảng đầu vào chứa pattern rỗng (`""`), `re.compile("")` sẽ được thực thi và khớp với mọi vị trí trong file mã nguồn. Điều này gây ra hàng nghìn vi phạm giả (false positives) và có thể gây treo hệ thống do quá tải bộ nhớ khi xử lý snippet. | Trình bày: SILENT_DATA_CORRUPTION | Trọng số: -5.0
- [Reliability] | [src/engine/verification.py:205] | Lý do: Logic trích xuất kết quả JSON sử dụng `stdout_lines[-1]`. Đây là phương pháp cực kỳ mong manh; nếu bất kỳ library nào được import hoặc script con in ra bất kỳ thông báo nào (Cảnh báo Deprecation, Log từ dependency) sau khi JSON đã được in, hệ thống sẽ cố gắng parse đoạn text đó thay vì JSON, dẫn đến crash toàn bộ tiến trình audit. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Performance] | [src/engine/scanners.py:28] | Lý do: Thuộc tính lớp `_pattern_cache` trong `RegexScanner` lưu trữ các đối tượng regex đã biên dịch vào dict mà không bao giờ giải phóng (eviction policy). Kết hợp với `natural_rules_compiler.py` cho phép tạo luật động liên tục từ ngôn ngữ tự nhiên, điều này sẽ tạo ra một rò rỉ bộ nhớ (memory leak) tiềm tàng trong các session auditing kéo dài hoặc service chạy liên tục. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/engine/symbol_indexer.py:52] | Lý do: Logic trích xuất mã nguồn sử dụng fallback `start + 15` nếu không tìm thấy `end_lineno`. Đối với các hàm/class dài (thường gặp trong dự án legacy hoặc God objects), việc chỉ lấy 15 dòng đầu tiên sẽ khiến AI ở các bước sau nhận dữ liệu thiếu hụt nghiêm trọng, dẫn đến việc xác nhận sai (False Negative) các lỗi logic nằm ở phần dưới của hàm. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Maintainability] | [tests/test_overall_pillars.py:56] | Lý do: Mâu thuẫn nghiêm trọng giữa giải thích toán học trong comment và giá trị được assert thực tế. Comment tính toán Score = 1.67 và 3.33 dựa trên công thức mật độ hình phạt, nhưng code lại assert giá trị 8.18 và 7.34. Điều này gây hiểu lầm nghiêm trọng cho các Auditor khác về logic tính toán thực tế của hệ thống và có thể che giấu lỗi logic trọng yếu trong lớp CodeAuditor. | Trình bày: MISLEADING_NAME | Trọng số: -2.0
- [Reliability] | [tests/test_natural_rules_compiler.py:31] | Lý do: Logic sửa lỗi JSON bằng cách ép thêm dấu ngoặc nhọn '{' và '}' (nếu thiếu '{') là cực kỳ không an toàn. Nếu AI trả về một mảng JSON [ ], văn bản rác, hoặc một phần của dictionary, logic này sẽ tạo ra một chuỗi không hợp lệ nhưng có thể gây nhầm lẫn hoặc lỗi dữ liệu (Silent Data Corruption) ở các bước xử lý sau. Điều này thường là biểu hiện của việc cố gắng che đậy lỗi sinh dữ liệu từ model AI thay vì xử lý triệt để. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Reliability] | [tests/test_overall_pillars.py:34] | Lý do: Sử dụng AsyncMock để ghi đè (monkey-patch) lên global singleton 'ai_service' mà không có cơ chế hoàn tác (teardown). Vì 'ai_service' là đối tượng chung của toàn bộ engine, việc sửa đổi trạng thái của nó trong một file test sẽ làm 'ô nhiễm' (pollution) các test case khác chạy cùng tiến trình, dẫn đến các lỗi không thể tái lập (Flaky tests). | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [tests/test_authorship.py:30] | Lý do: Truyền tham số git date vào subprocess.run dưới dạng string có chứa dấu ngoặc kép bọc ngoài: '--date="1 year ago"'. Do subprocess.run mặc định được gọi với shell=False, các dấu ngoặc kép này sẽ được coi là một phần của giá trị ngày tháng thô gửi tới git. Một số phiên bản git sẽ không thể parse được ngày này hoặc parse sai, khiến môi trường test trở nên mỏng manh và không ổn định tùy thuộc vào môi trường OS. | Trình bày: AI_REASONING | Trọng số: -3.0
- [Reliability] | [tests/test_authorship.py:10] | Lý do: Sử dụng các đường dẫn thư mục tạm cố định ('/tmp/test_authorship_project', '/tmp/test_discovery_project', v.v.). Điều này dẫn đến nguy cơ xung đột tài nguyên cực cao (Race Condition) khi chạy test song song hoặc lỗi quyền truy cập (Permission Conflict) trên Shared CI/CD Runners. Cách làm đúng là sử dụng thư viện 'tempfile.mkdtemp()' hoặc fixture 'tmp_path' của pytest. | Trình bày: AI_REASONING | Trọng số: -2.0
- [Reliability] | [src/api/routers/audit.py:42] | Lý do: Sử dụng 'threading.local' thông qua 'JobManager.set_active_job' trong môi trường bất đồng bộ (FastAPI) không đảm bảo tính nhất quán của bối cảnh. Do cơ chế thread-pool của AnyIO, các tác vụ từ các request khác nhau có thể chạy trên cùng một thread, dẫn đến việc ghi log bị lẫn lộn giữa các JobID. | Trình bày: AI_REASONING | Trọng số: -5.0
- [Maintainability] | [src/api/routers/audit.py:31] | Lý do: Hàm 'run_auditor_with_capture' mang tên 'capture' và tài liệu mô tả việc bắt stdout, nhưng mã triển khai thực tế chỉ gán Job ID cho log handler. Điều này khiến tên hàm trở nên vô nghĩa và gây nhầm lẫn về chức năng thực sự của nó (Misleading Name). | Trình bày: MISLEADING_NAME | Trọng số: -1.0
- [Security] | [src/api/routers/audit.py:272] | Lý do: Tham số 'request.snippet' trong endpoint 'get_fix_suggestion' được nhúng trực tiếp vào chuỗi prompt gửi tới AI. Người dùng có thể cố tình đóng khối code bằng ba dấu nháy ngược và chèn các chỉ thị mới để vượt qua các hạn chế hệ thống (Prompt Injection). | Trình bày: AI_REASONING | Trọng số: -6.0
- [Performance] | [src/api/routers/audit.py:128] | Lý do: Việc gọi 'await upload_file.read()' trên danh sách nhiều file tải lên cùng lúc mà không có giới hạn kích thước sẽ nạp toàn bộ nội dung vào RAM, có thể gây ra lỗi cạn kiệt bộ nhớ (OOM) cho máy chủ khi xử lý các dự án lớn. | Trình bày: AI_REASONING | Trọng số: -4.0
- [Reliability] | [src/engine/ai_service.py:84] | Lý do: Trong '_call_llm_json', biến 'content' được lấy từ phản hồi API nhưng không được kiểm tra giá trị khác 'None' trước khi xử lý sâu hơn hoặc trích xuất JSON. Điều này có thể gây ra lỗi AttributeError tại thời điểm chạy nếu mô hình trả về lỗi trống hoặc bị chặn. | Trình bày: UNCHECKED_NONE_RETURN | Trọng số: -5.0
- [Reliability] | [src/api/routers/audit.py:114] | Lý do: Logic gán 'project_name = safe_parts[0]' bên trong vòng lặp duyệt file upload sẽ khiến tên dự án bị ghi đè bởi thành phần đường dẫn đầu tiên của file cuối cùng. Nếu bộ file tải lên chứa các đường dẫn không đồng nhất, thư mục đích 'target_path' có thể bị xác định sai. | Trình bày: AI_REASONING | Trọng số: -3.0
